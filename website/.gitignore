node_modules/
build/
.docusaurus/
.cache-loader/

# The component reference is GENERATED from BlazorNative.Components' XML docs at
# build time and never committed (Phase 8.4 design, decision 3). The `///` next to
# the code is the one home for those facts; a committed copy here would be a second
# one, and a second copy is the thing this whole site is built to refuse.
docs/components/reference/
