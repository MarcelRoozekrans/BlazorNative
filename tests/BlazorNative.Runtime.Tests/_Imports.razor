@*
    ─────────────────────────────────────────────────────────────────────────
    _Imports.razor — Phase 13.0 Task 1: the project-wide Razor imports for
    ItemSurfaceProbe.razor, the first .razor file in this test project.

    BlazorNative.Components is the LIBRARY namespace — BnView and the
    FlexAlign/FlexPosition enums the probe sets. Mirrors
    samples/BlazorNative.SampleApp/_Imports.razor's BlazorNative.Components
    line (no @using Microsoft.AspNetCore.Components.Web here: the probe uses
    no @bind/@on* tag helpers, so that footgun does not apply yet).
    ─────────────────────────────────────────────────────────────────────────
*@
@using BlazorNative.Components
