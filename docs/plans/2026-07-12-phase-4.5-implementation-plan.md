# Phase 4.5 — NuGet Packaging + Consumer Smoke + M4 Close — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** five packages pack clean and a blank consumer project mounts a `Bn*`
component from packages alone (M4 DoD #7); the formal M4 audit closes the milestone
(DoD #8) → `v4.0`.

**Architecture:** per [2026-07-12-phase-4.5-design.md](2026-07-12-phase-4.5-design.md).
Version `1.2.0-phase-4.5` everywhere. nuget.org deferred. Merge via PR; tag post-merge.

---

## Global rules

- **Branch:** `phase-4.5-packaging` from `main` (controller creates).
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- **Environment:** the usual (NDK 26.3 / ANDROID_HOME / JAVA_HOME Adoptium 21 /
  VS-Installer PATH); AVD for the Gate 1 instrumented sweep.
- Baselines: .NET 203/0, JVM 73, Android 35 (counts should not change; version churn
  forces republishes + reruns).
- Read for context: design doc; all five csproj files + how ZeroAlloc.* deps are
  referenced (feed or local — step 0); `BlazorNative.Analyzers.csproj` (4.1's packaging
  notes: IncludeBuildOutput=false already?); `tests/BlazorNative.Renderer.Tests` harness
  (the smoke's mount shape); `Exports.cs` version const + both Kotlin assertion files;
  `ci.yml`; `.gitignore` (artifacts/); repo root for LICENSE (create MIT if absent —
  the repo is public!); `docs/plans/2026-07-10-milestone-3-final-audit.md` (audit
  template) + `docs/planning/MILESTONE.md` (the 8 DoD criteria + closure notes).

## Task 0: Branch

`git checkout -b phase-4.5-packaging`

---

## Gate 1 — Pack

### Task 1: Package metadata + version + three-surface sweep

0. Step 0: verify ZeroAlloc.* references resolve from a public feed (they're
   PackageReferences — confirm restore source). LICENSE check: create MIT LICENSE if
   absent (public repo hygiene — flag in report).
1. Pack metadata for Core/Renderer/Http/Components/Analyzers: PackageId, Version
   `1.2.0-phase-4.5`, Authors, PackageLicenseExpression MIT, RepositoryUrl,
   Description per project. Analyzers: `analyzers/dotnet/cs` layout,
   DevelopmentDependency; props/targets only if verified necessary (record outcome).
2. `dotnet pack -c Release -o artifacts/packages` → five .nupkg, ZERO warnings.
   Unzip-inspect the analyzers package (layout) + one library package (deps correct).
   `.gitignore` artifacts/.
3. Version const → `1.2.0-phase-4.5` + both Kotlin assertion files. Full sweep:
   win-x64 publish (4 IL2072s) → `dotnet test` 203/0 → JVM 73/0 → bionic republishes →
   filtered version-assertion instrumented test + full `connectedAndroidTest` 35/35.
4. Commit: `feat(phase-4.5): five NuGet packages at 1.2.0-phase-4.5 — analyzers layout verified (DoD #7 pack)`.

---

## Gate 2 — Consumer smoke

### Task 2: samples/ConsumerSmoke + script + CI step

1. `samples/ConsumerSmoke/`: own `nuget.config` (local `artifacts/packages` feed +
   nuget.org for transitive); console csproj referencing the five packages by version
   (NOT ProjectReferences; NOT in the sln); a hand-written component (BnView + BnText +
   BnButton w/ click) + Program mounting via the Renderer.Tests harness shape,
   asserting the expected patch set, PASS/exit 0.
2. Analyzer-activity pin: one deliberately-warning line (parameterless HttpClient in a
   `#if ANALYZER_TRIP` block or similar) + the script asserts BN0011 appears in build
   output when tripped and build is clean when not. Keep honest + simple.
3. `scripts/consumer-smoke.ps1` (house style): pack → clean restore (temp package
   cache, `--no-cache`) → build (capture the analyzer observation) → run → assert
   exit 0. Green locally.
4. `ci.yml`: add the smoke step (after JVM tests; windows job). Iterate on the PR
   (open it now — `phase-4.5-packaging` → main, title "Phase 4.5: NuGet packaging +
   consumer smoke + M4 close (closes #7)") to green.
5. Commit: `feat(phase-4.5): consumer smoke — blank project mounts Bn* from packages alone (DoD #7 proof)`.

---

## Gate 3 — Audit + close

### Task 3: Conclusion + audit-milestone + complete-milestone

1. `docs/plans/2026-07-12-phase-4.5-conclusion.md` (house style; nuget.org deferral +
   Runtime-not-packaged honesty; carryovers → M5/M6).
2. **audit-milestone**: walk ALL 8 M4 DoD criteria → `docs/plans/2026-07-12-milestone-4-final-audit.md`
   (M3 audit template): per-criterion PASS/FAIL + evidence; honesty notes (Runtime
   package, nuget.org, emulator-informational, DevHost mock session); scaffolding
   ledger; re-ledgered hardening items status; test counts at close.
3. On PASS: **complete-milestone** — ROADMAP M4 ✅ (date + audit link) + M5 pointer
   opened (P4 — Full Platform Coverage, mapped to BACKLOG; iOS shell + Android
   completeness + host-initiated nav/lifecycle); MILESTONE.md status → complete +
   closure notes; record the tag command for the controller:
   `git tag -a v4.0 -m "Milestone 4: P3 — Production-Shippable complete"` (post-merge).
4. Commit: `docs(phase-4.5): Phase 4.5 GREEN close-out + M4 final audit — DoD #7/#8 closed, milestone complete`.
5. Controller: final whole-branch review → PR CI green → merge → tag v4.0 → memory.
