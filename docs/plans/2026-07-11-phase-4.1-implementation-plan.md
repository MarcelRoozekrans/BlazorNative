# Phase 4.1 — Analyzer Rescope + Unit Tests — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** the BN analyzer set tells the truth about the NativeAOT runtime — 6 WASI-era rules
deleted, 5 survivors rescoped (M4 DoD #3), 2 new `[UnmanagedCallersOnly]` interop rules,
`Microsoft.CodeAnalysis.Testing` coverage for every rule, analyzer attached to all src/
projects with a zero-warning bar — merged via the new PR flow.

**Architecture:** rules consolidate into `MobilePolicyAnalyzer` (BN0004/0010/0011/0013),
`BridgeAsyncHandlerAnalyzer` (BN0014, reworded), `InteropBoundaryAnalyzer` (new BN0020/0021);
release-tracking files land, RS2008 NoWarn drops. Full contract:
[2026-07-11-phase-4.1-design.md](2026-07-11-phase-4.1-design.md).

**Tech Stack:** unchanged; version string stays `1.0.0-phase-3.5` (no runtime change).
Baselines: .NET 177/2 (grows this phase — ci.yml assertion updated in the same commit),
JVM 32, Android 32 (untouched).

---

## Global rules

- **Branch:** `phase-4.1-analyzers` from `main` (created by the controller). **Merge is via
  PR** — `main` is protected; no local merge.
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- Read for context: design doc; all three analyzer sources + csproj + test project;
  `src/BlazorNative.Runtime/Exports.cs` (the BN0020/21 reference conformer);
  `src/BlazorNative.Core/IMobileBridge.cs` + `DevHostBridge.cs` + `src/BlazorNative.Blazor/Pages/Home.razor`
  (BN0014's live subscribers); `.github/workflows/ci.yml` (the count assertion to update).

## Task 1 (Gate 1): Rescope + new rules, TDD

1. Tests FIRST for the target rule set (`tests/BlazorNative.Analyzers.Tests`): per design §4 —
   fires/silent/edges per rule; keep the 3 existing BN0014 tests green (they assert IDs via
   markup; if any asserts message text, update deliberately and note it).
2. Delete BN0001/0002/0003/0005/0006/0012 (and their now-empty registration paths).
3. Consolidate: `MobilePolicyAnalyzer.cs` (BN0004 warn, BN0010 warn, BN0011 warn narrowed to
   parameterless ctor, BN0013 error), reworded per design §1; `BridgeAsyncHandlerAnalyzer`
   reworded (drop Mono-WASI); new `InteropBoundaryAnalyzer.cs` (BN0020 exception-escape,
   BN0021 explicit cdecl + EntryPoint — both error). New categories `BlazorNative.MobilePolicy`
   / `BlazorNative.Interop`; help links →
   `https://github.com/MarcelRoozekrans/BlazorNative/blob/main/docs/analyzers.md#bn00xx`.
4. `docs/analyzers.md` — one section per surviving/new rule: what it flags, why, the
   compliant shape, the pragma escape hatch. Retired-IDs table at the bottom.
5. `AnalyzerReleases.Shipped.md` (release `1.0.0-phase-4.1`: removals + changes + new) +
   `AnalyzerReleases.Unshipped.md`; drop the RS2008 `<NoWarn>` from the csproj.
6. Full analyzer test suite green. Commit:
   `feat(phase-4.1): BN rules rescoped for NativeAOT — 6 retired, 5 reworded, BN0020/21 interop rules (TDD)`.

## Task 2 (Gate 2): Attach to src/ + zero-warning bar

1. Add `<ProjectReference Include="..\BlazorNative.Analyzers\BlazorNative.Analyzers.csproj"
   OutputItemType="Analyzer" ReferenceOutputAssembly="false" />` to Core, Renderer, Http,
   Runtime, Components, **and BlazorNative.Blazor** (six src projects).
2. Full solution build → **0 BN diagnostics**. Expected conformance check: `Exports.cs` all
   8 exports satisfy BN0020 (top-level try/catch) + BN0021 (explicit cdecl + EntryPoint) —
   if any export doesn't conform, make it conform (behavior-neutral) rather than pragma.
   Any other hit: fix or scoped pragma with justification comment.
3. Full `dotnet test` — record the NEW count (177 + new analyzer tests − 0; exact number).
   Update `.github/workflows/ci.yml`'s asserted totals in the SAME commit.
4. Quick JVM sanity NOT needed (no runtime/gradle change) — CI on the PR proves the rest.
5. Commit: `feat(phase-4.1): analyzers attached to all src projects — zero-warning bar (DoD #3)`.

## Task 3 (Gate 3): PR close-out

1. `docs/plans/2026-07-11-phase-4.1-conclusion.md` (house style; record the rule-by-rule
   fate table, new counts, any pragmas granted, carryovers: NativeEvents redesign still
   ledgered; analyzer NuGet packaging shape = Phase 4.5).
2. ROADMAP Phase 4.1 ✅ + MILESTONE DoD #3 closure note. Commit:
   `docs(phase-4.1): Phase 4.1 GREEN close-out — DoD #3 closed`.
3. Controller: final review → `gh pr create` → CI green (new count asserted) → merge.
