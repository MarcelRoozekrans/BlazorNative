# Phase 3.4 — Region Walk + Bn* Library + @bind + Cascading — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** The `Bn*` component library exists and works on Android — two-way bound input, live echo, and a cascading theme toggle — closing M3 DoD #4/#5/#6 after walking Region frames (the 3.3 MUST-FIX).

**Architecture:** `ProcessFrame` gains a transparent Region arm (no node, no slot — region-transparent sibling numbering); new public `BlazorNative.Components` project (BnView/BnText/BnButton/BnInput + BnDemo + BnTheme); the bind loop rides the 3.2 change plumbing into a new WidgetMapper `value` prop handler under the `applyingBatch` guard. Full contract: [2026-07-10-phase-3.4-design.md](2026-07-10-phase-3.4-design.md).

**Tech Stack:** unchanged pinned toolchain; version `0.9.0-phase-3.4`.

---

## Global rules

- **Branch:** Task 0 creates `phase-3.4-components` from `main`.
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- **Environment:** the usual (NDK 26.3 / ANDROID_HOME / JAVA_HOME Adoptium 21 / VS-Installer PATH).
- Baselines at branch: .NET 150/2; JVM 29; Android 29. Goldens (Hello + CompositionProbe shapes) contain no Regions — they are the Gate 1 regression net and must stay untouched.
- Read for context: design doc; `NativeRenderer.cs` `ProcessFrame` + `ProcessRenderTreeDiff` (the Region gap sits at the frame-walk `switch`; the 3.3 carryover block in `NativeWidgetTree.cs:86` names it); `BlazorInterop.cs` (`BnRenderTreeFrame` — needs `RegionSubtreeLength`); `RendererBlazorAPICoverage.cs` Probe5 (the cascading probe that currently "passes" only because it asserts values, not rendering); `TrimProbes.cs`/`CompositionProbe.cs` (probe/registry patterns); `HostSession.cs` registry; `WidgetMapper.kt` `handleUpdateProp`; `MainActivity.kt` EXTRA_COMPONENT; instrumented conventions (`CompositionAndroidTest.kt`).

## Task 0: Branch

`git checkout -b phase-3.4-components`

---

## Gate 1 — Region walk (.NET TDD)

### Task 1: RegionSubtreeLength + transparent descent

TDD in `DiffCursorTests.cs` (or a new `RegionWalkTests.cs` — prefer new file, this is a distinct concern):
- `CascadingValue_ChildContent_ElementsRender` — a hand-written parent using `builder.OpenComponent<CascadingValue<string>>` + ChildContent RenderFragment containing elements → the elements MUST produce CreateNodePatches parented correctly (today: silently dropped).
- `RenderFragment_Parameter_ChildrenRender` — a component with a `RenderFragment` parameter rendering it inline.
- `Region_ReRender_EditsResolve` — mutate state so the diff edits INSIDE the region (text change) → correct nodeId.
- `NestedRegions_SiblingNumbering` — region inside region + trailing sibling element → indices stay aligned.
- `Region_Prepend_MidList` — a region root arriving as a PrependFrame (the documented silently-dropped case) → children created at consecutive correct slots.
- `ComponentInsideRegion_GetsSlotAndParent` — a child component inside a region gains its slot + parent record (3.3's Region limitation on the component-parent map).
Implement: `BnRenderTreeFrame.RegionSubtreeLength`; Region arm in `ProcessFrame` (descend, same slot context); Region-root PrependFrame handling in `ProcessRenderTreeDiff`. Update the carryover ledger in `NativeWidgetTree.cs` (Region limit → FIXED). Full `dotnet test` — goldens untouched, strict green. Commit: `feat(phase-3.4): Region frames walked transparently — CascadingValue/RenderFragment render`.

### Task 2: Upgrade Probe5 + RendererBlazorAPICoverage honesty

The cascading probe currently asserts VALUE propagation only. Strengthen it (or add alongside) to assert the cascaded children's rendering + re-render on parent change. Also check `TrimValidationTests`' CascadingTestHost — does its ChildContent now render more patches (it used AddComponentParameter with a RenderFragment — the frame shapes may shift for tests that counted patches)? Fix any test that under-asserted. Commit: `test(phase-3.4): cascading probes assert rendering, not just values`.

---

## Gate 2 — Components project + BnDemo (.NET)

### Task 3: BlazorNative.Components project + the Bn* quartet (TDD)

New `src/BlazorNative.Components/BlazorNative.Components.csproj` (net10.0 library, Nullable, ImplicitUsings; PackageReference Microsoft.AspNetCore.Components 10.0.*; added to sln; Runtime gains a ProjectReference). Components per the design table (public, sealed, hand-written BuildRenderTree, gap-numbered sequences): `BnView.cs`, `BnText.cs`, `BnButton.cs`, `BnInput.cs` — BnInput's onchange invokes `ValueChanged` with the event value; document the @bind-mechanics posture in the class KDoc (the pair @bind-Value compiles to; Razor syntax = M6). TDD: new `tests/BlazorNative.Components.Tests` project? NO — keep tests in `BlazorNative.Runtime.Tests` (it has the renderer harness + strict defaults; add a ProjectReference to Components): `BnComponentTests.cs` — per-component mount shape via the Frames capture (BnView emits div+styles+children; BnButton attaches click; BnInput emits value attr + change handler; Enabled=false → UpdateProp). Commit: `feat(phase-3.4): BlazorNative.Components — BnView/BnText/BnButton/BnInput (DoD #4)`.

### Task 4: BnDemo + BnTheme + the bind loop headless (TDD)

`BnTheme.cs` (record: `Background`, `AltBackground` or minimal equivalent) + `BnDemo.cs` in Components (public; CascadingValue<BnTheme> wrapping a BnView form: bound BnInput, echo BnText, Clear BnButton, Theme BnButton toggling the cascaded theme). Register `"BnDemo"` in HostSession's registry. Tests (`BnDemoTests.cs` in Runtime.Tests, via TryMount + DispatchEventCore): mount shape; change-dispatch with payload "hello" → echo BnText re-renders "hello" (nodeId-pinned); Clear click → input value prop + echo reset; Theme click → SetStyle patches with the alt background on the themed children (cascading re-render — DoD #6 headless). Version const → `0.9.0-phase-3.4` + both Kotlin assertion files. Win-x64 publish → 4 accepted IL2072s only (the new project rides the annotated paths). Full `dotnet test` (record). Commit: `feat(phase-3.4): BnDemo — bind loop + cascading theme proven headless (DoD #5/#6 .NET)`.

---

## Gate 3 — JVM

### Task 5: BnDemo through the dll

New `BnDemoTest.kt` (JVM, boot + frames capture + dispatchEventBlocking): mount "BnDemo" → shape sanity (input present with value attr, echo text present); change dispatch with payload → echo ReplaceText nodeId-pinned; theme toggle click → SetStyle patches observed on multiple children. Kotlin compile surfaces need NOTHING new (no ABI change this phase) — confirm via the drift tests passing unchanged. `./gradlew testDebugUnitTest --rerun` (record; 29 + new). Commit: `feat(phase-3.4): Gate 3 GREEN — BnDemo bind + cascading through the dll`.

---

## Gate 4 — Android

### Task 6: WidgetMapper value prop + BnDemo as default + instrumented

1. `WidgetMapper.kt` `handleUpdateProp`: add `"value"` → `if (view is EditText && view.text.toString() != p.value) { view.setText(p.value ?: ""); view.setSelection(view.text.length) }` — inside applyBatch the `applyingBatch` guard already suppresses watcher dispatch; the inequality check prevents redundant setText; cursor-to-end + IME last-write-wins documented at the site.
2. `MainActivity.kt`: default component → `"BnDemo"`. **Check every instrumented test that launches MainActivity without EXTRA_COMPONENT** — WidgetMapperTest + EventRoundTripAndroidTest pin Hello's shape: they MUST now pass `EXTRA_COMPONENT="HelloComponent"` explicitly (per the design risk table). Update them.
3. Republish both bionic RIDs (4 IL2072s each).
4. New `BnDemoAndroidTest.kt` (ActivityScenario, default intent = BnDemo, strict via the established Os.setenv pattern — note the one-shot ordering contract: this class sorts before CompositionAndroidTest ("Bn" < "Composition") so IT now owns the first mount — move the @BeforeClass setenv here AND keep Composition's (idempotent; document)): type into the EditText on the UI thread → echo TextView updates on-screen (no dispatch echo loop — assert the input's text isn't clobbered); Clear button empties both; Theme button flips the BnView backgrounds (assert background color change on ≥2 children).
5. Full `connectedAndroidTest` (record; 29 + new), `testDebugUnitTest` (no regression), `dotnet test` (no regression).
Commit: `test(phase-3.4): Gate 4 GREEN — Bn* library live on the AVD (bind + cascading on-screen)`.

---

## Gate 5 — Docs

### Task 7: Close-out

Full three-surface sweep (record). `docs/plans/2026-07-10-phase-3.4-conclusion.md` (house style: gates/SHAs/counts; the Region walk as-built; the @bind-mechanics honesty note; carryovers: 3.5 — navigation service (DoD #7, the C-ABI exists since 3.1) + M3 final audit (probe-exports deletion incl. CompositionProbe fate + BnDemo stays as the demo); still-open ledger items from 3.2/3.3 that remain). ROADMAP: 3.4 ✅, **DoD #4/#5/#6 CLOSED**; next-pointer 3.5 (the LAST M3 phase). MILESTONE: the three checkmarks with dates + links. Commit: `docs(phase-3.4): Phase 3.4 GREEN close-out — DoD #4/#5/#6 closed`. Controller merges after final review.
