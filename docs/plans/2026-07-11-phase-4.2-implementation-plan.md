# Phase 4.2 — Runtime Hardening: Ledger Triage + Fixes — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** the runtime-hardening ledger becomes deliberate (M4 DoD #4): focus/blur wired
on-device, RouteChanged subscriber-isolated, the M1 allocation-budget test enabled with a
measured bound, the stale-watcher leak fixed — and the five re-ledgered items get a
written triage doc with rationale + revisit triggers.

**Architecture:** per [2026-07-11-phase-4.2-design.md](2026-07-11-phase-4.2-design.md).
Version → `1.1.0-phase-4.2`. Merge via PR.

---

## Global rules

- **Branch:** `phase-4.2-hardening` from `main` (controller creates).
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- **Environment:** the usual (NDK 26.3 / ANDROID_HOME / JAVA_HOME Adoptium 21 /
  VS-Installer PATH). AVD `blazornative-pixel6-x86_64` for Gate 3.
- Baselines at branch: .NET 197/2 (ci.yml asserts — update when counts change), JVM 32,
  Android 32. Eight exports; 4 accepted IL2072s per publish.
- Read for context: design doc §1 (fix designs — the survey file:line references are in
  it); `NativeNavigationManager.cs`; `DevHostBridge.RaiseNativeEvent` (the isolation
  pattern); `RendererSpike.cs` (both skipped tests + the ~512 B intent comment);
  `NativeRenderer.cs` `UpdateDisplayAsync`/`Mount` (the seam site) + `FrameArenaTests.cs`
  (the alloc-test pattern); `BnInput.cs` + `BnDemoTests.cs` (golden must not churn);
  `CompositionProbe.cs` + `HostSession.cs` registry (FocusProbe precedent);
  `WidgetMapper.kt` `handleAttachEvent`/`handleDetachEvent`/`watchers`;
  `EventRoundTripAndroidTest.kt`; `NavigationTests.cs` (subscriber-test home).

## Task 0: Branch

`git checkout -b phase-4.2-hardening`

---

## Gate 1 — .NET (TDD)

### Task 1: RouteChanged isolation + allocation test + focus carriers

1. **RouteChanged** (TDD in `NavigationTests.cs`): two subscribers, first throws →
   second runs, CurrentRoute updated, swap rc 0; strict-mode variant (contained +
   stderr-logged; document the posture per design). Implement via GetInvocationList +
   per-subscriber try/catch in `NativeNavigationManager`.
2. **Allocation test**: internal `NativeRenderer` seam to re-render a mounted root
   (design: TriggerRootRender or equivalent — smallest honest seam; document test-only
   posture). Un-skip `RenderWalk_IsAllocationFree_OnSteadyState`; warm + measure via
   `GC.GetAllocatedBytesForCurrentThread`; set the bound = measured baseline + sibling-
   style slack; record the measured number in the test comment AND the report.
3. **Stale skip**: run `NestedElements_EmitCreateNodeForEachLevel` unskipped — green →
   keep unskipped (update its comment); red → delete with a comment-trail note.
4. **Focus carriers**: `BnInput` OnFocus/OnBlur `EventCallback<FocusEventArgs>` (attach
   only when `HasDelegate` — KDoc); internal `FocusProbe` (Runtime, registry entry
   "FocusProbe", BnInput + echo BnText "focused"/"blurred", scaffolding KDoc like
   CompositionProbe). TDD: mount shape + dispatch focus/blur → echo nodeId-pinned;
   `BnDemoTests` golden asserted UNCHANGED (run it).
5. Version const → `1.1.0-phase-4.2` + both Kotlin assertion files (compile-check only).
   Win-x64 publish (4 IL2072s). Full `dotnet test` — record count; update ci.yml
   assertion in the SAME commit.
6. Commit: `feat(phase-4.2): RouteChanged isolation + allocation budget + focus carriers (DoD #4 .NET)`.

---

## Gate 2 — JVM

### Task 2: Focus/blur through the dll

Fresh win-x64 publish (Task 1 did). New `FocusBlurTest.kt` (JVM): mount FocusProbe,
harvest focus+blur handlerIds from AttachEvent patches, `dispatchEventBlocking` each,
assert echo ReplaceText transitions ("focused"→"blurred") nodeId-pinned. Version
assertion already updated. `gradlew testDebugUnitTest --rerun` — record count.
Commit: `feat(phase-4.2): Gate 2 GREEN — focus/blur round-trip through the dll`.

---

## Gate 3 — Android

### Task 3: WidgetMapper arms + stale-watcher + instrumented

1. `WidgetMapper.kt`: focus/blur attach arm (per-view focus/blur handlerId pair, single
   `setOnFocusChangeListener` dispatching whichever side is attached; detach clears the
   side, listener removed when both gone; document the single-listener semantics).
2. `WidgetMapper.kt`: `watchers` re-keyed handlerId → nodeId; attach removes prior
   watcher before adding; detach keys by node. Update the KDoc caveat (fixed).
3. New `FocusBlurAndroidTest.kt` (EXTRA_COMPONENT=FocusProbe): `requestFocus()` /
   `clearFocus()` on the real EditText via UI thread → poll echo TextView "focused" /
   "blurred". Extend `EventRoundTripAndroidTest.kt` (or WidgetMapper-level test) with
   the exactly-one-dispatch re-attach pin per design.
4. Republish both bionic RIDs (4 IL2072s each). Filtered new tests + full
   `connectedAndroidTest` — record. `dotnet test` + JVM no-regression re-run — record.
5. Commit: `feat(phase-4.2): Gate 3 GREEN — focus/blur live on the AVD + stale-watcher fixed (DoD #4 Kotlin)`.

---

## Gate 4 — Triage doc + close-out + PR

### Task 4: The written ledger + closure

1. `docs/plans/2026-07-11-phase-4.2-hardening-triage.md`: all 10 items (design table) —
   state (file:line), verdict, rationale, revisit trigger, ledger-of-record pointer.
   Update the in-code breadcrumbs of the 5 re-ledgered items to reference this doc
   (comment-only edits: `NativeRenderer.cs:927-930`, `BlazorNativeRuntime.kt:69-71`,
   `NativeWidgetTree.cs:172-175` + `358-364`, `NativeShellBridge.cs:304-308`).
2. `docs/plans/2026-07-11-phase-4.2-conclusion.md` (house style: gates/SHAs/counts,
   measured allocation number, the strict-posture decision on RouteChanged, FocusProbe
   scaffolding ledger entry, carryovers restated).
3. ROADMAP Phase 4.2 ✅; MILESTONE DoD #4 ✅ closure note.
4. Commit: `docs(phase-4.2): Phase 4.2 GREEN close-out — DoD #4 closed (4 fixed, 5 re-ledgered with rationale)`.
5. Controller: final whole-branch review → PR → CI green → merge.
