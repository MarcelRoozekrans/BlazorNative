# Phase 6.2 Design — Real Scrolling

**Date:** 2026-07-14
**Milestone:** M6 — Real-UI Foundation (closes DoD #4)
**Predecessor:** Phase 6.1 — Yoga owns all placement on both shells
([conclusion](2026-07-13-phase-6.1-conclusion.md), PR #58)
**Owns:** the `scroll` NodeType, stubbed since Phase 2.5 and explicitly ledgered as an honest
boundary by 6.1: *"`ScrollView` and `Spinner` are framework `ViewGroup`s that run their own
layout over their children — Yoga does not get the final word inside them."* This phase gives
Yoga the final word.

---

## Decisions (locked at brainstorm, 2026-07-14)

1. **Yoga's `overflow: scroll` on the scroll node** — not a shell-side derivation of content
   size. Yoga stays authoritative for every frame, including the off-screen ones. (Two shells
   independently unioning child frames is exactly where Android and iOS drift apart, and it is
   the class of bug the 6.1 drift tests exist to prevent.)
2. **Vertical only.** DoD #4 is "content taller than the viewport scrolls". Android's
   `ScrollView` is vertical-*only* — horizontal is a different widget class, chosen at
   `CreateNode` time from a `flexDirection` that arrives in a *later* `SetStyle` patch, which is
   an ordering problem for an axis no demo needs. Horizontal is ledgered.
3. **A new `/scroll` page** (`BnScrollDemo`). `BnLayoutDemo`'s 22-number frame table is the
   cross-platform parity contract; wrapping it in a scroll view would rewrite that table in the
   same phase that introduces the scroll engine, mixing engine bugs with golden churn.
4. **No scroll events.** `onScroll` fires at 60Hz and would be the first high-frequency producer
   on a wire designed for taps — it deserves its own design (coalescing, throttling), and its
   real customer is M7's virtualized list. Ledgered with `scrollTo` and offset-restore-across-nav.

## The model: a synthetic content node (the React Native shape)

A scroll view is a **viewport** with **content** that may exceed it. Yoga expresses that with two
nodes, and so do we:

```
scroll node          — the VIEWPORT. Its frame comes from flex, like any node.
                       overflow: scroll  → Yoga stops clamping the content to it.
  └─ content node    — SYNTHETIC (not a wire node). height: auto, width: 100%,
                       flexDirection: column. Its COMPUTED HEIGHT *is* the content size.
       ├─ child 0    — the wire children, laid out normally
       ├─ child 1
       └─ …
```

The content node is created by the shell whenever a `scroll` node is created, and the scroll
node's **wire children are inserted into the content node**, never into the scroll node itself.
Yoga computes the content node's height from its children (`height: auto`), and because the
scroll node is `overflow: scroll`, Yoga does **not** shrink it to the viewport. So
`contentSize.height` is read straight out of Yoga — no shell derivation, nothing for the two
platforms to disagree about.

The view tree mirrors it exactly:

| | Android | iOS |
|---|---|---|
| viewport | `ScrollView` (vertical) | `UIScrollView` |
| content | `BnYogaFrameLayout` (the single child `ScrollView` demands) | a plain `UIView` |
| contentSize | the content view's laid-out height | `scrollView.contentSize` ← the same |

This is the second index-mapping rule in the shells, alongside the text-collapse invariant, and
it is the same *kind* of rule: **the Yoga tree and the view tree mirror each other; neither
mirrors the patch tree.** A scroll node's child at wire index *i* is the content node's child at
index *i* — in both trees, on both platforms.

### Why this works on Android, and why 6.1 already paid for it

`ScrollView` measures its single child with an **`UNSPECIFIED` height spec** — "tell me how tall
you want to be". `BnYogaFrameLayout.onMeasure` already answers that correctly: 6.1's review fixed
it to **fall back to the last Yoga-applied size** under a non-`EXACTLY` spec (it used to report
0, which would have made the content vanish). So the content view reports its Yoga-computed
height, `ScrollView` accepts it, and scrolling follows. That fix was made for a boundary we were
only *documenting* at the time; it is load-bearing now.

### The constraint this introduces: a scroll node needs a definite height

An `auto`-height scroll node would size itself to its content and never scroll. So a `BnScroll`
must get its height from somewhere definite — an explicit `Height`, or `Grow="1"` inside a
bounded parent (the common case: a scroll filling the screen under the host root, whose height is
the host's). **Stated, tested, and logged**: a scroll node that ends up `auto`-height gets one
warning, because the symptom (a page that simply doesn't scroll) is otherwise baffling.

## The component

```csharp
<BnScroll Height="200">        // or Grow="1" in a bounded parent
    …children…
</BnScroll>
```

`BnScroll` emits the `scroll` element (already mapped: `MapElementToNodeType` sends
`scroll`/`overflow` → NodeType 6) and forwards the same flex parameter surface as `BnView` —
it *is* a flex item; it just scrolls its content. No new NodeType, no new patch kind, **no ABI
change** (still 9 exports + the 72-byte bridge).

## The proof surface: `BnScrollDemo` at `/scroll`

Every number predictable, so both shells assert the same ones (the 6.1 discipline):

- a **viewport** 300 wide × 200 tall
- **ten rows**, each `Height="80"` → content node computes to **800**
- therefore: `contentSize.height == 800`, viewport `200`, **scrollable range 600**
- **initial offset 0**; row 0's frame sits at y=0 *inside the content*, and rows 3+ are below the
  viewport's bottom edge
- **after scrolling to offset 600** (programmatically, in the test): the last row is visible and
  the content cannot scroll further

The load-bearing assertion is **`contentSize > viewport`, from Yoga** — that is the whole phase.
The scroll-and-assert step proves the viewport actually moves over the content, not merely that
the numbers add up.

## Verification (the GREEN bar)

1. `BnScrollDemo` computes **the same content size and the same row frames** on the AVD and the
   iOS simulator, asserted number-for-number (the 6.1 parity discipline).
2. Content taller than the viewport **actually scrolls** on both — asserted by driving the scroll
   position and observing the rows move relative to the viewport.
3. **An un-scrolled tree is unchanged**: `BnDemo`, `BnSettingsPage` and `BnLayoutDemo` keep their
   goldens and their frames on all four surfaces. The scroll work must not perturb the 6.1
   parity table.
4. A `scroll` node nested *inside* flex (and flex nested inside a `scroll`) both lay out —
   the demo does both.
5. CI counts bumped and asserted on every surface.

## Gates

| Gate | GREEN bar |
|---|---|
| **1 — .NET** | `BnScroll` + `BnScrollDemo` + the `/scroll` route; the patch golden (the contract Gates 2/3 assert frames against). No ABI change. .NET suite green. |
| **2 — Android** | `ScrollView` + the synthetic content node + `overflow: scroll`; wire children parented into the content view; the definite-height warning. `BnScrollDemo`'s content size, row frames and *actual scrolling* asserted on the AVD. |
| **3 — iOS** | The same via `UIScrollView` (+ `contentSize` from the content node) — **the same numbers as Gate 2**. Asserted on the simulator. |
| **4 — Close** | Conclusion + ledger; all lanes green with counts asserted; DoD #4 closed; PR merged. |

## Risks

| Risk | Mitigation |
|---|---|
| The synthetic content node desyncs the two trees' child indices (the 6.1 text-collapse class of bug) | One rule, stated once, mirrored in both shells: a scroll node's wire children go to the CONTENT node in *both* trees. Pinned by an insert-at-index test on both platforms |
| `ScrollView` re-measures/re-lays-out its child behind Yoga's back (the 6.1 `FrameLayout` lesson) | The content view is a `BnYogaFrameLayout` (layout suppressed); `ScrollView` gets its height from `onMeasure`'s Yoga fallback. Asserted by frames, not by types |
| Yoga's `overflow: scroll` semantics differ subtly from what we assume | The whole phase rests on it — so Gate 2 asserts the content node's computed height *directly* before any widget is involved, in a Yoga-only unit test |
| A scroll node with `auto` height silently never scrolls | Warned at runtime + a test pins the warning; documented in the component's XML doc |
| `RemoveNode` on a scroll node must free the synthetic content node too | The 6.1 subtree purge already walks the *Yoga* tree — the synthetic node is in it. Pinned by extending the node-lifecycle test (on iOS this is memory safety, not hygiene) |
