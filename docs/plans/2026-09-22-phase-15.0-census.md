# Phase 15.0 — the pin census

**Every caller of `BnRepo.Root()`, measured against `docs/pin-standard.md`.**

Milestone 15 exists because roughly two dozen drift pins accumulated with no shared standard, each
written to catch the bug in front of its author. Task 1 made the population enumerable, Task 2 gave
it one door, Task 3 wrote the rules. This is the measurement.

Its job is to size phase 15.1. A census that flatters the population plans the wrong phase; so does
one that condemns it. Every verdict below names the file, the fact, the assertion, and the reason,
so a reader can disagree with a specific judgement rather than with a mood.

---

## 0. The unit of classification is the fact, not the file

**Read this before the tables, because every number below is counted per fact.**

Rule 6 states the principle and `BnImageDemoTests` is its proof: five of that file's seven facts
never touch the checkout, and exactly two do. A single verdict on that file would be a lie in one
direction or the other.

So:

- **The population is enumerated by file** — `grep -rl "BnRepo.Root()"` is what makes it
  mechanically enumerable at all, and a helper called from a fixture puts the whole file on the
  list. That is Rule 6 working as designed.
- **The population is judged by fact.** A file appears in the scorecard as one row when all of its
  facts agree, and as several rows when they do not.
- **A single-verdict file is one whose facts agree** — it is not a file that was judged in bulk.

Two kinds of disagreement turned up, and they are different:

| Kind | Meaning | Count |
|---|---|---|
| **Mixed by kind** | the file holds both tree-reading facts and facts that never touch the checkout | **5 files** |
| **Mixed by verdict** | every fact reads the tree, but their verdicts differ | **3 files** |
| Single verdict | all facts agree | 19 files |

**8 of 27 files are mixed.** That is a minority, which is why the per-fact pass is a correction
rather than a re-census: nineteen files keep the verdict and the single row they already had.

---

## 1. The population, measured

```bash
grep -rl "BnRepo.Root()" tests/ --include="*.cs" | sort
```

**27 files, 118 facts, of which 102 are pins.** Measured on `feat/15.0-standard-and-census`, working
tree clean.

The file count is neither the milestone's opening guess of sixteen nor the first correction's
nineteen, and it is not a suffix match: it is the call graph. It agrees with the figure
`docs/pin-standard.md` quotes in Rule 8, and with `tests/Shared/BnRepo.cs`' own header, which records
24 copies of the walk before consolidation. The gap between 24 and 27 is the three files that
reached the tree through a neighbour's helper rather than their own copy.

Supporting counts, all re-measured rather than carried forward:

| Thing | Measured | Where it matters |
|---|---|---|
| Hand-written `.cs` under `tests/`, excluding `bin/` and `obj/` | **131** | `PinPopulationTests`' floor of 100 is set against 129, which is 131 minus its two by-name exclusions. The floor's stated denominator is correct. |
| Callers of `CommentStrippedSource` | **8** | Matches Rule 8's figure. One implementation, eight callers — the target shape. |
| `[Fact]` and `[Theory]` methods in the population | **118** | The denominator for every verdict below. |
| Of those, pin facts | **102** | 16 facts read no checkout content; see §2.3. |
| `dotnet test` gate | **1112** | Unchanged by this task, which writes only under `docs/`. |

**`RouteMenuDriftTests` is deliberately absent from this list and that is correct.** It asserts over
an in-memory page manifest and never reaches the tree, so it is outside Rule 6's population by
construction. The standard cites it in Rule 2 as the proof that vacuity is a property of the
assertion shape rather than of file access — it is an example the standard reasons *about*, not a
member it lost. Nothing in this census covers it, and 15.1 should not assume it is covered.

**That framing is too comfortable, and §9 corrects it.** Being outside the population by
construction is correct bookkeeping and it is also a hole: the enumeration key is *reads the tree*,
Rule 2's subject is *assertion shape*, and a vacuous-capable pin over two in-memory collections
falls in the gap between them. **Read §9 before quoting any count in this section as exhaustive over
pins** — they are exhaustive over tree-readers.

---

## 2. Which facts are pins

### 2.1 The test applied

Rule 1: *a pin is a test that reads the repository tree at runtime and asserts on its contents.*
Applied per fact, that excludes three shapes that appear inside population files:

- **facts asserting over build output.** Reading `BlazorNative.Components.xml` from
  `Path.GetDirectoryName(assembly.Location)`, or type names off a `.dll`, is not reading the
  repository tree. Rule 6 draws this line itself when it explains why `BnRepo.TestBinaryDirectory()`
  exists: *"`PackagePurityTests` genuinely needed its own build output rather than the repo tree"* —
  a different job, named separately, on purpose.
- **facts asserting over in-process state.** A baseline held against `SampleAppPages.All` is a
  content pin, but it is not a *tree* pin.
- **facts over a fixture the test writes itself.** A temp file created, fed to a parser, and deleted
  is a unit test of the parser.

A fact stays a pin when the checkout read is load-bearing even though it is mediated. The reference
fixtures run `scripts/generate-reference.ps1` **from the checkout** precisely so the pin cannot drift
from the lane — the file says a pin that re-implemented publish and generate *"could pass forever
while the lane pointed at bin/"*. The tree read is the point, so those facts are pins.

### 2.2 Two corrections to Rule 6's examples, now folded into the standard

Recorded here because they are what produced the per-fact unit.

**`TextCollapseParityDriftTests` is a pin, not golden support.** All four of its facts read
`WidgetMapper.kt` or `BnWidgetMapper.swift` and hold them against a behaviourally-derived harness
set. Its own header: *"This pin is what makes the new list a measurement instead of a claim."*

**`BnImageDemoTests` is half a pin, and the checkout reads are the pin half.** Its golden and
arithmetic facts never touch the tree. Every `BnRepo.Root()` call funnels through one helper,
`ShellSource`, whose only two callers hold **three transcriptions of four integers** —
`BnImageDemo`'s constants, `ImageFixtureServer.kt`, `BnImageFixtureServer.swift` — to one value.

### 2.3 The sixteen non-pin facts, named

**Silence is not a verdict, so each one is listed rather than summarised.**

| File | Fact | Why not a pin |
|---|---|---|
| `BnImageDemoTests` | `TheFrameTableIsTheContractsArithmetic_NotAProseNumber` | asserts over `BnImageDemo`'s own constants |
| | `TheFixtureContract_IsAFactOfTheConstants_NotOfACheckedInPng` | same |
| | `EverySectionIsAlignFlexStart_SoAMeasuredImageReportsItsOwnWidth` | asserts over mounted `RenderFrame` patches |
| | `Mount_Golden_ThreeImageCasesTheirSiblingsStylesAndSrcProps` | the golden proper |
| | `BackButton_NavigatesToTheDemoRoot` | in-process navigation behaviour |
| `ComponentReferenceDriftTests` | `EveryPublicComponent_CarriesATypeLevelSummary` | reads the shipped XML beside the **test binary**, plus reflection |
| | `EveryParameter_CarriesADocComment` | same |
| | `PublishedDocs_SpeakToConsumers_NotToTheRepo` | same — and this is where an uncontrolled detector lives, §5.3 |
| `PackagePurityTests` | `TheMovedRoster_IsPresentInTheSampleApp_EveryType` | asserts over PE metadata |
| | `TheMovedRoster_IsAbsentFromEveryShippedAssembly` | asserts over PE metadata |
| | `NoAppShapedTypeName_InAnyShippedAssembly` | asserts over PE metadata — the pattern net lives here, §5.3 |
| `RouteTableDriftTests` | `ManifestRoutedRows_MatchTheFourteenPageBaseline` | asserts over in-process `SampleAppPages.All` |
| | `TwoPagesSharingARoute_AreRefusedByTheGenerator_NamingBoth` | drives the generator over a self-written temp file |
| | `TheDuplicateRouteFixture_IsOtherwiseValid` | same — the negative control for the fact above |
| `WireVocabularyCodegenTests` | `TheManifest_RejectsAnUnknownTier` | drives the validator over an inline malformed manifest |
| | `AMalformedManifest_IsRefused_NotEmitted` | same |

**The most contestable call here is `PackagePurityTests`' three PE facts**, and it is flagged rather
than buried. They do touch the checkout — `ResolveAssemblyPath` reads the Analyzers csproj to
recover its TFM — but only to *locate* build output; the assertion subject is compiled metadata. A
reader who classifies them as pins would move one uncontrolled detector back inside the population
and change nothing else: all three conform as tests either way.

None of these sixteen is a defect. They are good tests doing a different job, and the point of
naming them is that **15.1 must not assume the pin standard covers them.**

---

## 3. The scorecard

Columns are the standard's rules. `Rule 2` asks whether the fact reds when its subject is absent.
`Rule 3` asks whether every *detector* in the fact has a fixed point it must still hit.

Files with one row are files whose facts agree. Files with several rows are the eight mixed ones.

| # | File / fact | Rule 2 | Rule 3 | Rule 4 | Rule 5 | Verdict |
|---|---|---|---|---|---|---|
| 1 | `AnalyzerDiagnosticRosterTests` — 2 facts | yes | yes | yes | yes | **conforms** |
| 2 | `AnalyzerHelpLinkDriftTests` — 2 facts | yes | yes | yes | partial | **conforms** |
| 3 | `ShellFrameTableDriftTests` — 5 facts | yes | yes | yes | yes | **conforms** |
| 4 | `ShellStyleTableDriftTests` — all 3 facts | ~~no~~ **yes** | ~~partial~~ **yes** | yes | ~~partial~~ **yes** | **fixed — 15.1 task 1** |
| 5 | `AndroidLogDriftTests` — 5 facts | yes | yes | yes | yes | **conforms** |
| 6 | `AuthSemanticsDriftTests` — 4 facts | yes | yes | yes | yes | **conforms** |
| 7a | `BnImageDemoTests` — the 2 fixture-server facts | yes | yes | yes | yes | **conforms** |
| 7b | `BnImageDemoTests` — the other 5 facts | n/a | n/a | n/a | n/a | **exempt — not pins** |
| 8 | `BnLogFormatDriftTests` — 9 facts | yes | yes | yes | yes | **conforms** |
| 9 | `BnSafeAreaCoverageTests` — 1 theory | yes | yes | yes | **fails green** | **gap** |
| 10a | `ComponentReferenceDriftTests` — the 2 generated-reference facts | yes | yes | yes | yes | **conforms** |
| 10b | `ComponentReferenceDriftTests` — the 3 shipped-XML facts | yes | partial | yes | yes | **exempt — not pins** |
| 11 | `ConsoleErrorDriftTests` — 4 facts | yes | yes | yes | yes | **conforms** |
| 12 | `DeepLinkSeedDriftTests` — 1 fact | yes | yes | yes | yes | **conforms** |
| 13a | `DispatchSurfaceDriftTests` — `EveryDeclaredMethod…`, `EveryMethodsLaneCall…` | yes | yes | yes | yes | **conforms** |
| 13b | `DispatchSurfaceDriftTests` — `EveryDispatchNamedDeclaration_IsDeclaredOrIgnored` | ~~no~~ **yes** | ~~no~~ **yes** | yes | yes | **fixed — 15.1 task 2** |
| 14a | `GeneratedSymbolShadowTests` — `EveryGeneratedSymbol_IsConsumed…` | yes | yes | yes | yes | **conforms** |
| 14b | `GeneratedSymbolShadowTests` — `NoGeneratedSymbol_IsShadowed…`, `NoProductionShellSource…` | yes | **no** | yes | yes | **gap** |
| 15 | `IosSliceMatrixDriftTests` — ~~1 fact~~ **2 facts** | yes | ~~partial~~ **yes** | yes | yes | **conforms** (clause 3 controlled by 15.1 task 2) |
| 16 | `NSLogDriftTests` — 4 facts | yes | yes | yes | yes | **conforms** |
| 17 | `NodeTypeCodomainDriftTests` — 2 facts | yes | yes | yes | yes | **conforms** |
| 18a | `PackagePurityTests` — the 3 shipped-set facts | yes | yes | yes | yes | **conforms** |
| 18b | `PackagePurityTests` — the 3 PE-metadata facts | yes | partial | yes | yes | **exempt — not pins** |
| 19 | `PackageVersionPinTests` — 6 facts | yes | yes | yes | yes | **conforms** |
| 20 | `PinPopulationTests` — 1 fact | yes | **no** | yes | **no** | **gap** |
| 21 | `ReadmeDriftTests` — 3 facts | yes | yes | yes | partial | **conforms** |
| 22 | `ReferenceDriftTests` — 8 facts | yes | yes | yes | yes | **conforms** |
| 23a | `ReleaseWorkflowPinTests` — `TheNugetApiKey_IsReferencedExactlyOnce…` | yes | yes | yes | yes | **conforms** |
| 23b | `ReleaseWorkflowPinTests` — `TheReleaseWorkflow_NeverOverridesTheVersion` | yes | ~~no~~ **yes** | yes | yes | **fixed — 15.1 task 2** |
| 24a | `RouteTableDriftTests` — `GeneratedRoutesJson…`, `AndroidDefaultFallbackLiteral…` | yes | yes | yes | yes | **conforms** |
| 24b | `RouteTableDriftTests` — the 3 in-process / temp-fixture facts | yes | yes | yes | yes | **exempt — not pins** |
| 25 | `TemplateDriftTests` — 11 facts | yes | yes | yes | yes | **conforms** |
| 26 | `TextCollapseParityDriftTests` — 4 facts | yes | yes | yes | yes | **conforms** |
| 27a | `WireVocabularyCodegenTests` — 9 manifest-reading facts | yes | yes | yes | yes | **conforms** |
| 27b | `WireVocabularyCodegenTests` — the 2 malformed-manifest facts | yes | yes | yes | yes | **exempt — not pins** |

### The spread, per fact

| Verdict | Facts | Of |
|---|---|---|
| **conforms** | **93** | 102 pin facts |
| **gap** | ~~9~~ ~~6~~ **4** | 102 pin facts |
| **fixed** | ~~0~~ ~~3~~ **5** | `ShellStyleTableDriftTests` (task 1); `DispatchSurfaceDriftTests`, `ReleaseWorkflowPinTests` (task 2) |
| **exempt — not a pin** | **16** | 118 total facts |
| **needs-mutation** | **0** whole facts; 1 sub-finding | §6.3 |

**As censused, the nine gap facts were:** three in `ShellStyleTableDriftTests`, two in
`GeneratedSymbolShadowTests`, and one each in `BnSafeAreaCoverageTests`,
`DispatchSurfaceDriftTests`, `PinPopulationTests` and `ReleaseWorkflowPinTests`.

**Phase 15.1 task 1 closed the three `ShellStyleTableDriftTests` facts** — the only *defect* among
the nine rather than a missing assertion. The detail is in §5.1, which is now a
record of a hole that was measured, closed and mutation-proven rather than one that is open.

**Phase 15.1 task 2 closed two more** — `DispatchSurfaceDriftTests`' bare completeness fact (issue
#357, open since 14.1) and `ReleaseWorkflowPinTests`' uncontrolled `VersionOverride`, and it
corrected the mislabel §5.5 warns about in the same commit. It also closed clause 3 of
`IosSliceMatrixDriftTests`, which was already a **conforms** row with a *partial* Rule 3. **Four
gap facts remain:** two in `GeneratedSymbolShadowTests`, one in `BnSafeAreaCoverageTests`, one in
`PinPopulationTests`.

### The same thing rolled up per file, for readers who want it

| | Files |
|---|---|
| All pin facts conform | **21** (**22** after 15.1 task 1; **24** after task 2) |
| At least one gap pin fact | ~~6~~ ~~5~~ **3** |
| …of which every pin fact is a gap | ~~3~~ **2** — `BnSafeAreaCoverageTests`, `PinPopulationTests`. `ShellStyleTableDriftTests` was the third and is **fixed** |
| …of which some pin facts conform | ~~3~~ **1** — `GeneratedSymbolShadowTests`. `DispatchSurfaceDriftTests` and `ReleaseWorkflowPinTests` were the other two and are **fixed** |

**On the verdict `fixed`, which was zero by construction and not by luck.** Task 4's constraint is
`docs/**` only: *do not fix any pin you find wanting — that is 15.1's job, and fixing while
censusing would leave the census describing a tree that no longer exists.* The slot the brief
reserves for "found wanting and repaired" is occupied by **gap**, defined as *read, judged against a
specific rule, found wanting, and left alone for 15.1.* No test file was touched **by the census**.

**Phase 15.1 is what moves a row to `fixed`,** and the three
`ShellStyleTableDriftTests` rows are the first to move. Rows are struck through rather than
rewritten so the measurement stays readable as a measurement: what was found, and what was then done
about it.

---

## 4. The two aggregate answers

Both are stated with the counting method attached, because both are what 15.1 is planned against.

### 4.1 Positive controls — the gap is real, narrower than the standard expects, and one shape

Rule 3 says positive controls are *"the one most existing pins fail"*. **Measured, that is not what
the population looks like.** A fact has a positive control if it contains an assertion requiring a
detector to still hit a known fixed point. Rule 3 lists three admissible forms and this census
honours all three, plus Rule 3's own concession that a **positive-match pin is controlled by
construction**: *"`DeepLinkSeedDriftTests` is a positive-match pin by construction: it asserts a
match exists rather than that none does, so its detector is exercised on every run."*

| Measure | Count |
|---|---|
| Files with **no** positive control anywhere | **1** — `PinPopulationTests` |
| **Uncontrolled detectors, total** | ~~9~~ **5** (task 1 closed one, task 2 closed three) |
| …**sitting in a pin fact** | ~~7, across 6 files~~ **3, across 2 files** |
| …sitting in a non-pin fact in a population file | **2** |

All nine, with the rewording that would send each one green:

| # | File · fact | Detector | Reword it and… | In a pin fact? |
|---|---|---|---|---|
| 1 | `PinPopulationTests` · the one fact | `BypassMarkers` | no offenders, forever. Nothing requires either marker to be found anywhere. | **yes** |
| 2 | ~~`ShellStyleTableDriftTests` · all 3~~ **CLOSED, 15.1 task 1** | the quoted-name extractor, **over-match direction** | a style name coinciding with a Yoga *value* keyword reads as a dispatch arm. Demonstrated on a real name — `gap`, a live `yogaStyles` entry, was in the Swift body's collected set from a commented-out line, though never compared against a set it could collide with. §5.1. | **yes** |
| 3 | ~~`DispatchSurfaceDriftTests` · `EveryDispatchNamedDeclaration…`~~ **CLOSED, 15.1 task 2** | `DispatchNamedDeclaration` | zero declarations found, completeness guard green over nothing. §5.2. | **yes** |
| 4 | `GeneratedSymbolShadowTests` · `NoGeneratedSymbol_IsShadowed…` | the shadow declaration regex | a hand-written twin stops being seen; `#279` reopens silently. | **yes** |
| 5 | `GeneratedSymbolShadowTests` · `NoProductionShellSource…` | the seam call-site pattern | a production bypass call stops being seen. | **yes** |
| 6 | ~~`IosSliceMatrixDriftTests` · the one fact~~ **CLOSED, 15.1 task 2** | the job-level `if` / `continue-on-error` pattern | a conditioned matrix leg passes. One clause of an otherwise strongly controlled fact. | **yes** |
| 7 | ~~`ReleaseWorkflowPinTests` · `TheReleaseWorkflow_NeverOverrides…`~~ **CLOSED, 15.1 task 2** | `VersionOverride` | a version override in the release path passes. §5.5, and it is mislabelled. | **yes** |
| 8 | `ComponentReferenceDriftTests` · `PublishedDocs_SpeakToConsumers…` | the nine banned prose patterns | published repo-speak passes. The file carries the scar, §5.3. | no |
| 9 | `PackagePurityTests` · `NoAppShapedTypeName…` | `AppShapedTypeName` | the *next* demo page parked in a shipped assembly passes. | no |

**Every one of the nine is an absence assertion.** That is the finding: *the population's
positive-control gap is exactly its absence-assertion surface.* A pin that asserts a match exists is
controlled for free. A pin that asserts nothing matches needs a fixed point bolted on, and nine such
assertions do not have one.

**All nine are worth closing** — the last two are not pins, but an uncontrolled detector is a
liability regardless of which population it sits in, and both live in files 15.1 will already have
open. What the classification buys is that 15.1 knows which two are outside the pin standard's
remit and should not be scoped as drift-pin work.

This reshapes the phase. It is not "add a detector to most of twenty-seven pins". It is **nine
fixed-point assertions**, six of them copyable from `ConsoleErrorDriftTests`, `NSLogDriftTests` and
`AndroidLogDriftTests`, which solved this exact problem for this exact assertion shape.

**Item 6's anchor, recorded because "no honest anchor exists" was a permitted answer and this is not
it.** `IosSliceMatrixDriftTests`' clause 3 forbids a job-level `if:` or `continue-on-error:` on the
matrix job, and 15.1 task 2 controls it with **`ci.yml`'s own aggregator**: `ios-build` carries
`if: ${{ always() }}` at job level, and that line is load-bearing rather than incidental — a
`needs:` job with no `if:` is skipped the moment its dependency fails, so without it the aggregator
would never run in exactly the case it was written to report. It cannot be deleted while the job
still does its job. The negative half is also a real line in the tree: `ios-build-slice` carries a
**step**-level `if: always()` at eight spaces, inside the very body the pin scans, which the
four-space anchor must keep refusing — so both directions are held by the workflow rather than by a
fixture. Only the `continue-on-error` alternative has no instance anywhere in `ci.yml`; adding a
conditioned job so a test could find one would be changing the subject to suit the pin, so that one
clause gets a labelled fixture and the difference is written at the pin.

### 4.2 Anti-vacuity — four facts, and the "nothing at all" claim was too strong

**This number moved, and the per-fact pass is what moved it.** See §8.

| Severity | Facts | Instances |
|---|---|---|
| **Can pass while scanning nothing** | ~~4~~ ~~1~~ **0** | ~~3 in `ShellStyleTableDriftTests`~~ (floored by 15.1 task 1), ~~1 in `DispatchSurfaceDriftTests`~~ (floored by 15.1 task 2) |
| …executing **no** anti-vacuity assertion at all | **0** | — |
| Floored on the set they iterate | ~~98~~ ~~101~~ **102** | every pin fact |

All four **as censused** had the same shape, and it is not "no floor" — it is **a floor on the wrong
side of the comparison**:

- `ShellStyleTableDriftTests` floors `dispatched`, the set being *subtracted*, and leaves `routed`,
  the set being *iterated*, unfloored.
- `DispatchSurfaceDriftTests.EveryDispatchNamedDeclaration_IsDeclaredOrIgnored` calls `Surface()`,
  which does assert `methods.Count >= 4` — so an anti-vacuity assertion genuinely executes inside
  that fact. It guards the **manifest**, not the **scan**. The scanned set, the `dispatch`-prefixed
  declarations in the two runtime sources, has no floor, so the `foreach` can run zero times with
  the assertion above it satisfied.

The actionable finding is unchanged: four facts can go green over nothing, and the fix is a floor on
the iterated set. Only the label moved, and it moved toward a sharper description of the defect.

**15.1 task 2 applied the same fix to the fourth**, in `DispatchSurfaceDriftTests`, so the count is
now zero. `TheDispatchDeclarationScan_IsNotVacuous` floors the two shells separately at 6 and 4
against measured 8 and 5, and the contrast was **observed, not asserted**: with
`DispatchNamedDeclaration` reworded to match nothing, the new floor reds while
`EveryDispatchNamedDeclaration_IsDeclaredOrIgnored` — the fact whose loop just ran zero times —
**passes**. That green is the defect, printed.

**15.1 task 1 applied that fix to the three `ShellStyleTableDriftTests` facts.**
`FloorTheIteratedSet` now guards `YogaStyleAttributes` at 20 — measured 26, six names of headroom —
and `VisualStyleAttributes` at 3, which is the measured count because a three-name set has no
headroom to leave. The contrast was **observed, not asserted**: with `routed` collapsed to empty and
the floor in place the fact reds naming the count; with the floor removed it goes green. The one
instance that remained, in `DispatchSurfaceDriftTests`, is closed by task 2 above.

---

## 5. The gaps, argued

### 5.1 `ShellStyleTableDriftTests` — the floor was on the wrong set, and the over-match was live · **CLOSED by 15.1 task 1**

**Status: CLOSED.** Both findings below were confirmed by measurement and then fixed; the section is
kept in its original tense so the argument that found the hole survives, with the resolution stated
at the end of each half. **This was the only *defect* among the nine gap facts** — the other six are
missing assertions over correct code.

All three facts, same two findings. The second is the one to act on.

**The floor guards the wrong side.** Every fact has the shape:

```csharp
var routed = NativeRenderer.YogaStyleAttributes;
var dispatched = ParseNameTable(KotlinYogaLayout, KotlinSetStyleBody);
var missing = routed.Except(dispatched).ToList();
```

`ParseNameTable` ends with `Assert.NotEmpty(names)`, so the *subtracted* set is floored. `routed`,
the *iterated* set, is not. `NativeRenderer.YogaStyleAttributes` and `VisualStyleAttributes` are
built from `src/wire-vocabulary.json`; if either came back empty, `routed.Except(dispatched)` is
empty and all three facts go green over nothing.
`WireVocabularyCodegenTests.TheRenderersStyleSets_AreTheManifests` would catch the collapse
elsewhere — but that is cross-coverage, not this pin's own floor, and Rule 2 is about the assertion
shape in front of you.

**The extractor over-matches, demonstrably, today.** `ParseNameTable` collects *every* quoted string
in the function body. Extracted from `YogaLayout.setStyle` right now, alongside the real arm names,
are `"center"`, `"flex-start"`, `"flex-end"`, `"row"`, `"row-reverse"`, `"column"`,
`"column-reverse"`, `"nowrap"`, `"absolute"`, `"relative"` and two multi-line log message strings.
None is a dispatch arm; all are in `dispatched`.

The consequence has a concrete shape. Yoga's *value* vocabulary overlaps its *property* vocabulary,
so a manifest name colliding with a value keyword — `gap`, `row`, `column`, `wrap`, `center` are all
live candidates — would read as dispatched on the strength of a `flexDirection` value literal, with
no arm written. That is precisely the failure this pin exists to catch, reachable through the pin
itself.

Rule 5 settles how seriously to take it: *"A limit that can only cost you a red is a footnote. A
limit that can hand you a green is a defect, whether or not it is written down."* This one hands you
a green, and it is not written down — the doc comment discloses a different and smaller limit, that
the pin proves the arm is *written* rather than *reached*.

Rule 3's second form is the fix shape: **a known-mismatching subject the detector must still
reject.** `RouteTableDriftTests.TheDuplicateRouteFixture_IsOtherwiseValid` is the worked example
already in the tree.

#### What 15.1 task 1 did about it

**Measured first.** `YogaLayout.setStyle`'s body yields **48** distinct quoted strings against
**26** real arms; `WidgetMapper.handleSetStyle` **6** against **3**; `BnWidgetMapper.handleSetStyle`
**28** against **3**. The census's five names were confirmed and it had undercounted: the full
non-arm set for the Yoga body is the fifteen `when (value)` keywords (`row`, `column`,
`row-reverse`, `column-reverse`, `flex-start`, `center`, `flex-end`, `space-between`,
`space-around`, `space-evenly`, `nowrap`, `wrap`, `wrap-reverse`, `relative`, `absolute`) plus seven
log-message fragments — **22**, not seven.

**And it found the defect operating on a real name rather than a hypothetical one — which is a
weaker claim than it first looks, and the weaker claim is the true one.** The Swift visual body
carries a comment whose worked example reads `OpenElement("scroll") + AddAttribute("gap", …)`, and
`gap` is a live `yogaStyles` entry. So a genuine manifest style name **was present in that body's
collected set, put there by a commented-out line** — the noise the section describes is not
hypothetical noise, and it is not limited to value keywords.

**It never flipped an assertion, and the section should not be read as saying it did.** Every
consumer of the Swift body's set was traced, in that file and repo-wide: it is compared **only**
against `VisualStyleAttributes` — `backgroundColor`, `color`, `fontSize` — in
`AppleSetStyleDispatch_HasAnArmForEveryVisualStyle`, and **never** against `YogaStyleAttributes`.
iOS's Yoga half is pinned at Swift runtime by a separate mechanism that never touches this regex. So
`gap` sat in the noise set without ever being checked against a name it could collide with, and was
masking no wrong green on any assertion that existed on `main`.

**Which is exactly the point.** Whether a stray name in the bag flips an assertion depends on which
set the bag happens to be compared against — luck, not design — and that is the reason to stop
collecting them rather than to reason case by case about whether each one currently matters.

**Fixed in both halves, because neither is sufficient alone.** `ParseNameTable` now anchors on
arm-label position for the file's language — Kotlin `"name" ->`, Swift `case "name":`, both read off
the shell sources — and keeps only the shallowest-indented arms, which is what separates a top-level
arm of `when (property)` from a nested arm of `when (value)`. And
`TheNameExtractor_CollectsArmLabelsOnly_NotEveryQuotedString` is the negative control: each measured
non-arm must still be **present** in the body as a quoted string, and must be **absent** from the
parsed set. Widen the extractor back and it reds naming the name.

**Mutation-proven, one at a time, reverted between.**

| Mutation | Result |
|---|---|
| Drop `"top"` from `setStyle`'s `"top", "right", "bottom", "left" ->` arm label, leaving the nested `when (property)`'s `"top" -> YogaEdge.TOP` in place | **RED**, naming `top`. The pre-15.1 extractor was run against the same tree and went **GREEN** — the false green reproduced, so the red is attributable |
| Widen the extractor back to every quoted string | negative control **RED**, naming `row` |
| Collapse `routed` to empty, floor present | **RED** on the floor |
| Collapse `routed` to empty, floor removed | **GREEN** — the vacuity contrast, observed |
| Line-comment an arm out (`// "flexGrow" -> …`) | **RED**, naming `flexGrow`. The old extractor passed this; the narrowing closed it |
| Block-comment an arm out (`/* … */`) | **GREEN** — a surviving limit, disclosed at the pin, unchanged from before 15.1 |

**Arm counts did not move.** 26 / 3 / 3 before the narrowing and 26 / 3 / 3 after, with zero names
appearing that the old extractor had not collected. The narrowing removed 22 / 3 / 25 non-arm
strings and no arms.

**One limit survives and is disclosed at the pin rather than here.** An arm inside a `/* … */` block
is still collected, which is a false-green direction. It is bounded by inspection — none of the
three bodies contains a `/*` — and the real fix is Rule 8 consolidation onto `CommentStrippedSource`,
which is string-literal-aware and already handles Kotlin and Swift, but lives in
`BlazorNative.Runtime.Tests` rather than `tests/Shared`. **That move is a candidate for a later
15.1 task and is deliberately not in task 1's scope.**

### 5.2 `DispatchSurfaceDriftTests` — one fact of three, the 14.1 residual — **CLOSED, 15.1 task 2**

`EveryDispatchNamedDeclaration_IsDeclaredOrIgnored` iterates `DispatchNamedDeclarations(source)`,
which is a `Regex.Matches` projection. If the regex stops matching — a reformat putting a newline
between the method name and its paren would do it — the loop runs zero times and the fact passes
while scanning nothing. The `Surface()` floor it inherits guards the manifest, not the scan; see
§4.2.

Its two siblings conform and keep their verdict. **This is a mixed-by-verdict file: two facts good,
one bare.**

Partial mitigation, recorded so 15.1 does not over-scope:
`EveryDeclaredMethod_ExistsInEveryShellItClaims` asserts a per-method `Regex.IsMatch` equality over
at least four declared methods, and that cousin pattern would red under most of the same reformats.
The hole is real but narrower than it reads.

The file's own header names the principle the bare fact violates: *"A method this pin cannot
classify ... reds loudly naming the method, rather than passing vacuously. That is the exact failure
mode #339 exposed in the first place: a pin that goes quiet on what it cannot see."*

**CLOSED BY 15.1 TASK 2 — issue #357.** `TheDispatchDeclarationScan_IsNotVacuous` floors the
**scanned** set, per shell, at 6 Kotlin and 4 Swift against measured 8 and 5. The floor doubles as
the Rule 3 control for `DispatchNamedDeclaration`: a pattern that must still hit six real
declarations in one source and four in the other cannot be reworded past its subject quietly. Two
notes the fix records at the pin rather than here. It floors the shells **separately**, because a
summed floor is satisfiable by Kotlin's eight alone and would pass over an emptied Swift scan. And
the floor lives in a **sibling** fact, which is `AuthSemanticsDriftTests`' shape and is what makes
the mutation contrast legible: break the pattern and the floor reds while the completeness fact
itself still passes. The completeness check cannot floor its own walk without asserting the thing it
is trying to discover, so the two are read together — stated in the pin's doc comment, not inferred.

### 5.3 The two uncontrolled detectors that turned out not to be in pins

**`ComponentReferenceDriftTests` is no longer a gap, and the reason is structural rather than
exculpatory.** Its two generated-reference facts — the ones whose checkout read is load-bearing —
are well floored and controlled by named fixed points: `BnView`, `BnText` and `BnButton` must be
among the reflected components, and the generated page set is compared both directions. They
conform.

The uncontrolled detector is the nine banned prose patterns in
`PublishedDocs_SpeakToConsumers_NotToTheRepo`, which reads the XML beside the **test binary** and
never touches the checkout. That fact is well floored for what it is — `members.Count > 50`,
`published.Count > 50`, and a named fixed point proving the XML loaded — but the nine patterns
control nothing. The file records the exact failure this invites, in its own comment:

> *NO trailing `\b`, and that is not sloppiness — it is a correction. The phrase in the shipped XML
> was "the BnDemo goldens stay byte-identical", and `/\bgolden\b/` does NOT match "goldens" ... The
> mutation caught 3 of 4 patterns and this was the miss.*

A detector that failed to match its subject, found by mutation, on this assertion. The scar is
attached; the control is not. **Still worth fixing — just not as drift-pin work.**

**`PackagePurityTests` is likewise clean on its pin half.** Its three shipped-set facts —
`TheShippedSet_IsExactlyTheSrcCsprojs`, `TheConsumerSmokeScript_PacksExactlyTheShippedSet`,
`TheConsumerSmokeProject_ReferencesExactlyTheShippedSet` — read the checkout, compare sets both
directions against an enumeration of `src/`, and floor every parse. They conform with no
sub-finding. The uncontrolled `AppShapedTypeName` pattern net lives in
`NoAppShapedTypeName_InAnyShippedAssembly`, a PE-metadata fact.

### 5.4 `GeneratedSymbolShadowTests` — excellent floors, two uncontrolled absence detectors

`GeneratedSymbolFloor = 14` is a model floor: measured, itemised as 5 Swift plus 6 Kotlin plus 3 C,
asserted inside the one helper both shadow pins iterate, with an explicit note that a silent parse
failure would green *both*.

`EveryGeneratedSymbol_IsConsumed_OrAllowlistedWithAReason` is positive-match and therefore
controlled — it **conforms**, and keeps that verdict. The other two do not:

- **`NoGeneratedSymbol_IsShadowedByAHandWrittenDeclaration`.** Nothing asserts the declaration regex
  still matches a known declaration — and the fixed point exists in the tree already, since
  `BnFrameAdapter.swift`'s forwarding declaration of `nodeTypes` must be *matched as a declaration
  and then exempted by the forwarding window*. Nothing asserts either half fired. That is also a
  Rule 7 concern: the forwarding window is the suppression branch, and no assertion proves anything
  ever enters it.
- **`NoProductionShellSource_CallsTheHostEventSeamsDirectly`.** Floors the walk
  (`sources.Length > 0`) but nothing requires the seam call-site pattern to still match anywhere.
  The seam's legitimate call sites are in test source sets the pin excludes by design, so the fixed
  point would have to be built rather than borrowed.

### 5.5 `ReleaseWorkflowPinTests` — one fact controlled, one mislabelled — **CLOSED, 15.1 task 2**

`TheNugetApiKey_IsReferencedExactlyOnce_AndOnlyInReleasePleaseYml` is among the best-controlled
assertions in the population: `total == 1` means a reworded `NugetApiKeyReference` yields zero
matches and **reds**. An exact-count structural assertion is Rule 3's third form, exactly. It
**conforms**.

`TheReleaseWorkflow_NeverOverridesTheVersion` does not, and it says it does. The assertion labelled
**"THE POSITIVE CONTROL, first"** is:

```csharp
Assert.True(source.Contains("dotnet nuget push", StringComparison.Ordinal), …);
```

That proves the *subject* is still the release path. It proves nothing about the `VersionOverride`
regex, which is the detector. Measured against Rule 3's definition — *a fixed point the detector is
required to hit* — it is a Rule 4 subject-moved guard wearing Rule 3's name.

**This mislabel is why it is called out rather than folded into the list.** A 15.1 sweep that greps
for the phrase "positive control" to find what is already covered will score this fact as done. It
is not. Whatever mechanism 15.1 uses to find the other detectors must read the assertion, not the
comment above it.

**CLOSED BY 15.1 TASK 2, both halves.** The comment now reads *THE SUBJECT-MOVED GUARD (pin standard
Rule 4)* and says in as many words that it is not a positive control and why the two get confused,
so the trap is disarmed for the next sweep rather than merely noted here. The real control is
`TheOverrideDetector_StillMatchesTheShapeItWasWrittenFor`.

**It is a FIXTURE, deliberately, and the search is recorded because a bare "none exists" would be
the same unpinned claim the standard warns about.** No tree anchor is honest here. A live
`-p:Version=` in a build path does not exist **by construction** — the pin's whole claim is that the
repo never overrides the version, so its subject tree is required to be empty of the pattern, which
is the structural difference from `NSLogDriftTests`, whose exempt bundle must still hold what the
shipped tree must not. The four places that do spell the shape are
`scripts/release-preflight.ps1`'s comment, `docs/GITHUB-SETUP.md`'s prose, the archived
`2026-07-16-phase-8.2-*` plans, and `ci.yml`'s own test-count narration — all prose, all reworded
freely and legitimately, and a control anchored to a sentence buys a false red on a docs edit. The
standard's own corollary about floors that move for irrelevant reasons applies to controls too: a
guard that reds for a reason nobody can connect to a real change is one the next person weakens
rather than investigates.

So the fixture is built as close to the real thing as a fixture gets: it splices the reference
implementation's own `pack -p:PackageVersion=$VERSION` into the **real** workflow text immediately
above the **real** `dotnet nuget push` command line, runs the pin's own offender projection over the
result, and requires exactly one offender **at the line the splice landed on** — so the failure
message's line fidelity is exercised, not only the regex. It carries the alternate spellings the
regex's doc comment promises and a near-miss negative, `-p:VersionPrefix=` and `-p:VersionSuffix=`,
which a widened pattern must still refuse. What a fixture cannot buy is stated at the pin: it proves
the detector recognises the shape, never that it is pointed at anything real, and that second half
is what the subject-moved guard buys. The two are only worth anything read together — which is
exactly the distinction the old label collapsed.

### 5.6 `PinPopulationTests` — no control, and its own limit is not where the standard says it is

Two findings on the mechanism that makes the population enumerable at all.

**No positive control, and the natural fixed point is excluded by name.** The pin asserts an empty
offender list. Nothing requires either `BypassMarkers` entry to be found anywhere — so a mistyped
marker, or a `CommentStrippedSource` change that strips too aggressively, leaves it green forever.
The standard is candid about the floor's scope: *"including its author's own floor of 100 — proves
only the walk."*

The sharp part is where the obvious fixed point went. `tests/Shared/BnRepo.cs` contains **both**
markers in live code — it is the one permitted implementation of the walk — and it is one of the two
files the pin skips by name. **The one file guaranteed to hold the detector's subject is the one
file the detector never reads.** That is not an argument for removing the exclusion; `BnRepo.cs`
must stay exempt from the offender check. It is an argument that the exemption and the control are
two different jobs, and only one was done — which is `NSLogDriftTests`' whole design, in reverse:
*"The exempt test bundle is the fixed point that proves the detector detects."*

**Rule 5's disclosure is not at the pin.** Rule 5 states the limit and asserts where it lives: *"That
is written down where the pin is, with a worked demonstration, rather than inferred."* Grepping
`PinPopulationTests.cs` for `adversarial`, `concatenat`, `determined`, `limit`, `does not cover` and
`cannot see` returns **nothing**. The file's header covers the definition of a pin, why the scan runs
over stripped code, and the two by-name exclusions. The accidental-versus-determined boundary and
the string-concatenation bypass are disclosed only in `docs/pin-standard.md`. Cheap to fix; worth
fixing, because Rule 5 is specifically about a reader finding the limit where they find the guard.

### 5.7 `BnSafeAreaCoverageTests` — a disclosed limit that fails green

The smallest gap, included because Rule 5's test is about direction rather than size.

The pin greps five named files for the literal `"BnSafeArea"`. Its floor is structural and sound — a
literal five-row `TheoryData`, each row asserting `File.Exists` before the content check — and the
assertion is positive-match, so the detector is controlled. It discloses its limit honestly:

> *It cannot tell an active wrap from a wrap sitting inside a `@* comment *@` — that is a real, small
> gap.*

But trace the direction. Delete the wrap and the file stops containing the string: **red**, correct.
Comment the wrap out and the file still contains the string: **green**, while the page renders under
the notch — which is `#338` restored. Rule 5: *"A limit that can only cost you a red is a footnote.
A limit that can hand you a green is a defect, whether or not it is written down."*

Eight sibling pins route their scans through `CommentStrippedSource` for exactly this reason. This
one does not — and cannot directly, since the shared stripper handles C#, Kotlin and Swift comments,
not Razor's `@* *@`. That makes it a small piece of real work rather than a one-line reuse, and
sizing it that way is the point of saying so.

---

## 6. Things that are not verdicts

### 6.1 The enforcement blind spot

`PinPopulationTests` excludes two files **by name, never by pattern**:

```csharp
if (name == "BnRepo.cs" || name == "PinPopulationTests.cs") continue;
```

Both exclusions are correct and neither should be removed. `BnRepo.cs` is the one permitted
implementation of the walk; `PinPopulationTests.cs` is where the markers are declared, and nothing
can scan for a string it is forbidden to contain.

**The blind spot is what follows: a bypass written inside either file is invisible to enforcement.**
In rising order of plausibility:

1. A test class declared *inside* `PinPopulationTests.cs` could walk to the solution file by hand
   and never be seen. Implausible, but it is the exclusion read literally.
2. **`BnRepo.cs` could grow a second walk.** This is the one that matters. `BnRepo.Root()` and
   `BnRepo.TestBinaryDirectory()` already coexist there — deliberately, and the second one's doc
   comment explains that routing `PackagePurityTests`' legitimate base-directory use through the
   helper is what kept the marker list at its original width. A third method added the same way,
   with a slightly different sentinel or climb, would be **two copies of one truth inside the file
   whose entire purpose is that there be one** — Rule 8's twin-divergence precondition, sitting
   inside the mechanism built to prevent it, in the one location no scan reads.

Nothing currently guards that. `BnRepo.cs` is 51 lines and reviewed by eye. 15.1 should decide
whether that is sufficient and write the answer down either way; the census's job is to make sure
nobody discovers the exclusion by surprise.

### 6.2 What "conforms" does and does not claim

It claims the fact satisfies Rules 2 through 6 as far as reading the source can establish. It does
**not** claim Rule 7 — *its mutations must exercise the pin's own code paths* — which is a property
of work performed, not of code that can be read. Several files record their mutation history
(`PackageVersionPinTests`' Gate B, `ComponentReferenceDriftTests`' four-pattern mutation,
`ReleaseWorkflowPinTests`' first-run red on a YAML comment). Many do not. **No Rule 7 column appears
because this census cannot honestly fill one in**, and a column of guesses would be worse than its
absence. If 15.1 wants Rule 7 measured, that is a separate exercise with a test runner attached.

### 6.3 One needs-mutation sub-finding, and it sits in a non-pin fact

**`PackagePurityTests.ResolveAssemblyPath` — a possible stale-build-output channel.**

When an assembly is not beside the test binary, the pin resolves it from the checkout's own build
output directory. That is whatever was last built there, and it can be arbitrarily old.
`File.Exists` reds when the file is *absent*; nothing reds when it is *stale*.

If reachable: a demo page added to `BlazorNative.Analyzers` today would not appear in a dll built
last month, so `TheMovedRoster_IsAbsentFromEveryShippedAssembly` and
`NoAppShapedTypeName_InAnyShippedAssembly` would pass over old metadata. Whether it is reachable
depends on CI build ordering and on whether `build-test` guarantees a fresh build of the Analyzers
project — **runtime state this census cannot evaluate by reading**, which is the condition the brief
says to mark rather than guess at.

**Both affected facts are the PE-metadata ones, so this lands outside the pin population.** The
file's three pin facts do not call `TypeNamesOf` at all. Marked `needs-mutation` regardless; the
experiment is cheap: build, edit a type name in the Analyzers source, run the suite without
rebuilding that project, see whether the pin notices.

### 6.4 Three smaller observations

- **`AnalyzerHelpLinkDriftTests`' heading floor is not measured.** `anchors.Count > 5` runs against a
  page with 9 headings outside code fences. It clears at 67% of its subject, and Rule 2's corollary
  asks for *a measured floor with stated headroom*. Not theatre the way `>= 20` against 131 was, and
  the fact's main assertion is positive-match and therefore controlled — but the number came from
  nowhere in particular, and the corollary exists because such numbers get argued down.
- **The population's two best negative controls both sit in non-pin facts.**
  `RouteTableDriftTests.TheDuplicateRouteFixture_IsOtherwiseValid` exists solely so the
  duplicate-refusal test cannot be satisfied by the extractor throwing on every input, and
  `WireVocabularyCodegenTests`' two malformed-manifest facts drive the validator over three bad
  shapes asserting specific message substrings. Both are Rule 3's second form, built deliberately,
  and both are the pattern §5.1 needs — **so the model for the hardest fix in 15.1 is already in the
  tree, just outside the pin population.**
- **`WireVocabularyCodegenTests`' nine pin facts are controlled on their own**, independently of
  those two: exact ordered host-event list, exact reserved list, byte comparisons behind measured
  floors, `Contains` assertions on emitted enum text. Reclassifying the validator pair costs the
  file nothing.

---

## 7. What 15.1 is sized at

In rough order of value per unit of work.

| Priority | Item | Scope |
|---|---|---|
| 1 | ~~Floor the iterated set in `ShellStyleTableDriftTests`, and add a negative control for the live over-match~~ **DONE — 15.1 task 1** | one floor plus one fixed point; the only demonstrated false-green channel in the population, and it covered 3 of the 9 gap facts. Suite 1112 → **1113** |
| 2 | ~~Floor `DispatchSurfaceDriftTests.EveryDispatchNamedDeclaration_IsDeclaredOrIgnored`'s scanned set~~ **DONE — 15.1 task 2** | one fact; the 14.1 residual and issue #357, closed. Floors 6 Kotlin / 4 Swift against measured 8 / 5 |
| 3 | The ~~seven~~ **three remaining** uncontrolled detectors sitting in pin facts | one fixed-point assertion each; six borrowable from `ConsoleErrorDriftTests` / `NSLogDriftTests` / `AndroidLogDriftTests`. Task 1 closed one, task 2 closed three — `IosSliceMatrixDriftTests` and `ReleaseWorkflowPinTests` from this row, plus item 3 from row 2. `GeneratedSymbolShadowTests` ×2 and `PinPopulationTests` remain |
| 4 | `PinPopulationTests` — a positive control, plus its Rule 5 disclosure moved to the pin | the enforcement mechanism should not be the least-controlled pin in the population |
| 5 | ~~`ReleaseWorkflowPinTests`' mislabelled "positive control" comment~~ **DONE — 15.1 task 2** | corrected in the same commit as its detector, exactly as this row asked |
| 6 | `BnSafeAreaCoverageTests`' Razor-comment false green | needs Razor comment handling, so real work rather than reuse |
| 7 | The two uncontrolled detectors in non-pin facts | worth closing, scoped as doc-coverage and purity work rather than drift-pin work |
| 8 | Decide and record whether `BnRepo.cs`' by-name exemption needs a guard of its own | §6.1 |
| 9 | Resolve the `PackagePurityTests` stale-dll question by experiment | §6.3 |

**93 of 102 pin facts conform as censused; 96 after 15.1 task 1; 98 after task 2.** The population is in better shape than the milestone's framing
assumed, and the gaps cluster tightly enough to be closed by one mechanism rather than dozens of
edits: **every uncontrolled detector in the repo is an absence assertion, and the fix for an absence
assertion is a fixed point it must still hit.** One lesson, four worked examples already in the
tree, nine places to apply it.

---

## 8. What the per-fact pass changed

The first cut of this census judged 27 files and carried one verdict each. Rule 6's correction — *the
unit of classification is the fact, not the file* — was applied in a second pass over notes already
taken, not a re-census. Nineteen files were unaffected. Four numbers moved:

| Number | Was | Is | Why |
|---|---|---|---|
| Verdict spread | 20 conforms / 7 gap / 1 partial exempt, per **file** | 93 conforms / 9 gap / 16 exempt, per **fact** | the unit changed; the underlying judgements did not |
| Files with a gap | 7 | **6** | `ComponentReferenceDriftTests` leaves. Its two pin facts conform; its uncontrolled detector is in a fact that reads build output, not the tree. §5.3 |
| Uncontrolled detectors | 9, across 8 files | **9**, of which **7 in pin facts across 6 files** | none disappeared. Two were reclassified as sitting outside the pin population, and both remain on the 15.1 list |
| Facts with **no** anti-vacuity property at all | 1 | **0** | overstated. `DispatchSurfaceDriftTests`' bare fact does execute `Surface()`'s `methods.Count >= 4` — it guards the manifest rather than the scanned set. Four facts can still pass while scanning nothing; §4.2 |

**Nothing was reclassified into safety.** The two detectors that left the pin population are still
listed, still counted, and still scheduled. The one number that genuinely softened — "no
anti-vacuity property at all", 1 to 0 — softened because the first claim was wrong, and the per-fact
reading is what caught it.

---

## 9. What this census cannot see — the enumeration key has a hole

**Read this alongside §1, because it is a limit on that section's completeness rather than a
footnote to it.** Rule 5 applies to a census as much as to a pin.

**The population is enumerated by `grep -rl "BnRepo.Root()"`, so the population is *tree-readers*.**
That is what makes it mechanically enumerable at all, and Rule 6 says so plainly. But "reads the
repository tree" is Rule 1's definition of a pin, and **Rule 2's subject is larger than Rule 1's
definition**:

> *Vacuity is a property of the assertion shape, not of the input. An assertion of the form* for
> every X, assert Y *passes trivially when there are no X — and it does not matter whether X came
> from a regex over source, a directory walk, a manifest parse, or an in-memory collection.*

**A test that compares two in-memory collections never calls `BnRepo.Root()`.** It can hold two
copies of one truth, red when they diverge, carry every property this document measures — and be
invisible to the key. The enumeration is not merely incomplete in the way any sample is; it is
**blind in exactly the direction a vacuous-capable pin sits.**

§1 already names the live instance and treats it as an example the standard reasons *about*. That
framing was too comfortable, and this section is the correction:

**`RouteMenuDriftTests` is the demonstration, not the exception.** It asserts
*every routed page, except two exemptions, has a menu row* over an in-memory page list. The standard
cites it in Rule 2 precisely because it would pass over an empty list. So the one test the standard
uses to prove that vacuity is independent of file access is also the one test **no mechanism this
milestone built can see**:

| Mechanism | Sees `RouteMenuDriftTests`? | Why not |
|---|---|---|
| `BnRepo.Root()` call graph — the population key | **no** | it never reaches the checkout |
| `PinPopulationTests` — Rule 6 enforcement | **no** | it scans for tests that reach the tree *without* the helper; one that never reaches the tree is correct by that rule |
| This census | **no** | its scope is §1's caller list |
| Any future guard keyed on the helper | **no** | same key, same hole |

**What it means for 15.1.** Two things, and they are separable:

1. **The number `102 pin facts` is a count of tree-reading pin facts.** Every verdict in §3 stands;
   none of them is disturbed. But the denominator is not "every pin in the repo" and should not be
   quoted as one. How many non-tree pins exist is **unmeasured** — `RouteMenuDriftTests` is the one
   this census happened to walk past, and Rule 1's four-for-four scar is the reason to assume it is
   not alone.
2. **A second enumeration would need a different key**, and no cheap one is known. Tree-reading is
   decidable by a call-graph grep; *compares two copies of one truth* is not — it is the same
   binding problem that sank mechanical Rule 2 enforcement in `docs/pin-standard.md`, arriving from
   the other side. **15.1 should decide and record whether the Rule 6 population is the population
   it wants**, and if the answer is no, what the second key is. Recording the decision matters more
   than the outcome: an undisclosed hole in an enumeration is how a census becomes folklore.

**No verdict changes because of this section.** Its job is to make sure nobody plans against §1's
counts believing them to be exhaustive over pins, when they are exhaustive over *tree-readers*.
