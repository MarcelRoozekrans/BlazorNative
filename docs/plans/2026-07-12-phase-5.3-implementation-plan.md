# Phase 5.3 — Swift Shell Interactivity — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** the two-page BnDemo runs interactively on the iOS simulator — bound input +
live echo, Clear, cascading theme, Settings⇄Back navigation — the Android v3.0 bar
(M5 DoD #3).

**Architecture:** per [2026-07-12-phase-5.3-design.md](2026-07-12-phase-5.3-design.md).
**All additions are Swift + ios.yml — no .NET/Kotlin/Android change** (the exports/
bridge/dispatch already exist). Merge via PR.

---

## Global rules

- **Branch:** `phase-5.3-swift-interactivity` from `main` (controller creates).
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop. **iOS is
  CI-only** — write the Swift correct-first against the survey's pinned contracts; each
  macOS run uploads everything (xcodebuild log, simulator log, xcresult); >6 reds on one
  step = stop.
- **Do NOT change any shared code** — nothing outside `src/BlazorNative.Apple/` +
  `.github/workflows/ios.yml` + the GITHUB-SETUP doc-lag. If a shared change seems
  needed, STOP and report (it means a scoping error). Confirm by `git diff --stat`
  showing only those paths.
- Baselines: .NET 220 / JVM 78 / Android 38 (untouched — no shared change); version
  stays `1.3.0-phase-5.1`. iOS XCTest 2 → ~5-7 (recorded, asserted in ios.yml).
- Read for context: the design doc + the Phase 5.3 survey's exact contracts —
  `WidgetMapper.kt` (handleAttachEvent/Detach `:160-265`, the value write-back
  `:354-361`, the maps/cleanup); `BlazorNativeRuntime.kt` (dispatchEvent + the lane
  `:82-161`); `Exports.cs` (dispatch_event rc contract `:234-332`, register_bridge
  `:343-358`); `BridgeProtocolNative.cs:60-69` (the 6-callback struct) + `ShellBridge.kt`
  (writeUtf8 `:235-242`, the -needed protocol) + `AndroidShellBridge.kt` (the bridge to
  mirror); `BnDemo.cs`/`BnSettingsPage.cs` (the components); `BnDemoAndroidTest.kt` +
  `NavigationAndroidTest.kt` + `EventRoundTripAndroidTest.kt` (the test pins to mirror);
  the existing `src/BlazorNative.Apple/` shell (BnFrameAdapter/BnWidgetMapper/BnRuntime/
  the C header/ios.yml — what 5.2 built).

## Task 0: Branch

`git checkout -b phase-5.3-swift-interactivity`

---

## Gate 1 — Interactive two-page demo on the simulator

### Task 1: Dispatch lane + event wiring + bridge + write-back + nav + interactive XCTests

1. **Dispatch lane (b)**: add the serial `DispatchQueue("BlazorNative-Dispatch")` to
   `BnRuntime`; a `dispatchEvent(handlerId, name, payload?)` async-submitting to it,
   calling `blazornative_dispatch_event`, routing non-zero rc to the error sink; a
   blocking test seam (for XCTests) mirroring `dispatchEventBlocking`. The FlatJson args
   writer (`{"name":"click"}` / `{"name":"change","payload":text}`, exact escaping).
2. **Event wiring (a)**: `BnWidgetMapper` gains the `onUiEvent` closure seam (wired to
   `BnRuntime.dispatchEvent`); AttachEvent → `UIButton .touchUpInside` (payload nil) /
   `UITextField .editingChanged` (payload text), last-wins (remove prior target first),
   per-nodeId tracking, identity cleanup on RemoveNode; focus/blur via `.editingDidBegin/
   DidEnd`; DetachEvent removes. Handle the 4 BnDemo AttachEvents that already arrive in
   the mount frame (5.2 skipped them — now wire them).
3. **Bridge (c)**: `AppleShellBridge` — all 6 `@convention(c)` callbacks (singleton-
   routed, no capture, throw→-1, process-lifetime); navigate + current-route real (route
   slot lock/atomic-guarded, seeded "/", the -needed buffer protocol per writeUtf8);
   storage×3 + fetch honest stubs. Register via `blazornative_register_bridge` BEFORE
   mount in the boot sequence.
4. **Value write-back (d)**: the value UpdateProp arm — `if textField.text != newValue {
   textField.text = newValue; cursorToEnd() }`, NO applyingBatch guard (document why:
   UIKit doesn't fire .editingChanged on programmatic set).
5. **Navigation + theme (§4)**: ride the existing render path — verify a Theme tap flips
   backgroundColor on form+echo and a Settings tap swaps to BnSettingsPage (RemoveNode+
   CreateNode); no new mapper code, but confirm the event-triggered re-render frames take
   the CommitFrame→main hop.
6. **Interactive hosted XCTests (e)**: bind+echo (type "héllo→世界" → echo==typed + input
   not clobbered), clear, theme flip (both directions), Settings→ (title + no textfield),
   ←Back fresh remount. Drive via `sendActions`. Poll the UIView tree.
7. **`ios.yml`**: update the asserted XCTest count (2 → the new total). Iterate to green
   on the branch (the workflow runs on pull_request + workflow_dispatch since 5.2; use a
   temporary bootstrap push trigger ONLY if needed for pre-PR iteration, removed before
   merge). Each red diagnosed in the fixing commit; upload everything. Record the working
   config + wall times + the final XCTest count.
8. Commit(s): `feat(phase-5.3): interactive two-page BnDemo on the iOS simulator (DoD #3)`
   (+ diagnosis commits).

---

## Gate 2 — Close-out + PR

### Task 2: Doc-lag fix + close-out

1. **GITHUB-SETUP.md iOS-lane doc-lag** (the 5.2 carryover): add the iOS-lane
   promotion-criteria section (mirroring the emulator lane) + update the stale "iOS rows
   return when the Swift shell lands in M5" placeholder / add the iOS PR-template row now
   the shell has landed.
2. `docs/plans/2026-07-12-phase-5.3-conclusion.md` (house style; the interactive shell
   as-built (a-e), the `@bind` iOS-simplification (no applyingBatch), the AppleShellBridge
   + the -needed protocol, the XCTest pins + count, the "no shared change" scope;
   carryovers: clipboard/share = 5.4, real-device/signing = later, storage/fetch bridge
   stubs → real when a component needs them, focus/blur wired-but-BnDemo-unused).
3. ROADMAP Phase 5.3 ✅ (house density: interactive two-page demo on the sim, the v3.0
   parity, XCTest count, links). MILESTONE DoD #3 ✅ closure note (bound input + echo +
   theme + Settings⇄Back on the simulator — iOS reaches the Android v3.0 bar). Issue #17
   comment (iOS shell interactivity done; remaining iOS = device/signing later).
4. Confirm no shared-code change (`git diff main..HEAD --stat` — only Apple/ + ios.yml +
   GITHUB-SETUP + docs/plans). No .NET/JVM/Android no-regression needed (nothing shared
   changed) — but state that explicitly.
5. Commit: `docs(phase-5.3): Phase 5.3 GREEN close-out — DoD #3 closed`.
6. Controller: final whole-branch review → PR → CI (build-test) green + the ios lane
   green → merge.
