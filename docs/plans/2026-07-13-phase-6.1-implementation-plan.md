# Phase 6.1 Implementation Plan — Flexbox Layout Core

> **For Claude:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development` — fresh implementer subagent per task, spec review then code-quality review after each.

**Goal:** Yoga stops being a probe and becomes the engine: every node lives in a Yoga tree,
containers are plain `FrameLayout`/`UIView`, every child is placed at a computed frame, and
leaves are measured natively — identically on Android and iOS.

**Design:** [2026-07-13-phase-6.1-design.md](2026-07-13-phase-6.1-design.md) (read it — the four
locked decisions and the layout model are there and are not up for re-litigation).

**Architecture:** Typed C# flex params → stringified onto the **existing** `SetStyle` wire → each
shell maps the string to a Yoga style setter → one `calculateLayout` per committed frame → frames
applied to views. **No ABI change** (9 exports + the 72-byte bridge, untouched).

**Tech stack:** .NET 10 / NativeAOT · Kotlin + `com.facebook.yoga:yoga:3.2.1` (JNI bindings) ·
Swift + Objective-C++ over Yoga's C-API (`libyoga.a`, built from source in `ios.yml`).

**Branch:** `phase-6.1-flexbox-core` (already created; the 6.0 close-out docs + this design ride on it).

---

## Non-negotiables (violating any of these fails spec review)

1. **No C-ABI / wire change.** No new export, no patch-struct change, no bridge-struct change.
   Flex props ride `SetStyle` (kind 6). CI asserts 9 exports on 3 RIDs — keep it that way.
2. **iOS: Yoga's headers must never be visible to Swift.** All Yoga interop lives in
   Objective-C++ (`.mm`) behind a plain-C surface. Xcode's Swift explicit-module dependency
   scanner reads the bridging header with a path-less search honouring neither
   `HEADER_SEARCH_PATHS` nor `-Xcc -I`. This cost six red CI runs in 6.0 — do not re-learn it.
3. **The text-collapse invariant.** A `text` node whose parent is a text-bearing non-container
   gets no view *and must get no Yoga node* — otherwise the two trees' child indices skew.
4. **An un-styled tree must emit no new patches and still stack vertically.** All new `BnView`
   params default to null → no attribute → no patch. `BnDemoTests.cs` / `BnDemoTest.kt` goldens
   must not churn.
5. **Yoga computes in density-independent units on both platforms.** iOS points 1:1; Android
   multiplies by `density` at frame-apply time only. One conversion site.

---

## Gate 1 — .NET: typed flex params, the wire, the demo page

### Task 1.1 — The style enums + `BnView` flex params

**Files:** create `src/BlazorNative.Components/FlexStyles.cs`; modify `src/BlazorNative.Components/BnView.cs`;
test `tests/BlazorNative.Runtime.Tests/BnComponentTests.cs`.

TDD:
1. Write failing tests: `BnView` with `Direction=FlexDirection.Row, Grow=1` emits exactly the
   `SetStyle` patches `flexDirection="row"` and `flexGrow="1"`; a `BnView` with **no** flex params
   emits **no** flex `SetStyle` patches (the un-styled invariant, non-negotiable #4).
2. Run → fail.
3. Implement: enums `FlexDirection {Row, Column, RowReverse, ColumnReverse}`, `Justify {FlexStart,
   Center, FlexEnd, SpaceBetween, SpaceAround, SpaceEvenly}`, `Align {Auto, FlexStart, Center,
   FlexEnd, Stretch, Baseline}`, `Wrap {NoWrap, Wrap, WrapReverse}`, `Position {Relative, Absolute}`;
   each with a `ToStyleValue()` returning the CSS-cased string (`RowReverse → "row-reverse"`,
   `FlexStart → "flex-start"`). `BnView` gains the nullable params from the design's style-surface
   table, each emitted via `AddAttribute` only when non-null.
4. Run → pass. Commit.

**Watch:** the attribute *sequence numbers* in `BuildRenderTree` must stay stable/unique — Blazor
diffing depends on them. Keep the existing `backgroundColor`(1)/`padding`(2) numbers and append.

### Task 1.2 — Renderer allow-list + the null-reset fix

**Files:** modify `src/BlazorNative.Renderer/NativeRenderer.cs` (`StyleAttributes` ~line 1029; the
removed-attribute path ~line 589); test `tests/BlazorNative.Renderer.Tests/` (new
`StyleResetTests.cs` + extend the existing attribute-classification tests).

TDD:
1. Failing test A: every new style name (`flexGrow`, `flexShrink`, `flexBasis`, `alignSelf`,
   `alignContent`, `flexWrap`, `gap`, `rowGap`, `columnGap`, `position`, `top`, `right`, `bottom`,
   `left`, `minWidth`, `maxWidth`, `minHeight`, `maxHeight`) classifies as `SetStylePatch`, not
   `UpdatePropPatch`.
2. Failing test B (**the bug**): a component that renders `FlexGrow=1` and then re-renders with
   `FlexGrow=null` must emit `SetStylePatch("flexGrow", null)` — today it emits
   `UpdatePropPatch("flexGrow", null)` and the shell would never reset the value.
3. Run → fail (B fails for the right reason: assert the patch *kind*).
4. Implement: extend `StyleAttributes`; at the removed-attribute site branch on
   `StyleAttributes.Contains(name)` → `SetStylePatch(nodeId, name, null)`.
5. Run → pass. Commit.

### Task 1.3 — `BnRow` / `BnColumn`

**Files:** create `src/BlazorNative.Components/BnRow.cs`, `BnColumn.cs`; test `BnComponentTests.cs`.
Thin presets over `BnView` (`Direction=Row` / `Column`), forwarding every other param. Test: `BnRow`
emits `flexDirection="row"`; an explicit `Direction` on `BnRow` is not silently overridden (decide
and pin: the preset **wins** — a `BnRow` is a row; do not expose `Direction` on it at all).
**No `BnStack`** (design decision 3).

### Task 1.4 — `BnLayoutDemo` + the `/layout` route

**Files:** create `src/BlazorNative.Components/BnLayoutDemo.cs`; modify the mount/route registry
(find it: `HostSession` + the shells' registry — mirror how `/settings`→`BnSettingsPage` is wired,
**all mirrors must move together**); test `tests/BlazorNative.Runtime.Tests/BnLayoutDemoTests.cs`.

The component (design §"The proof surface") — keep every number predictable:
- a **row** (width 300, height 100): box 50 · `Grow=1` · box 50
- a **column** with `Justify=SpaceBetween` and one `AlignSelf=Center` child
- a **wrap** row whose children overflow to a second line
- a **text** leaf whose measured size drives its frame
- a "← Back" button (nav parity with the other pages)

Test: the emitted patch list golden (creates + `SetStyle`s + insert indices). This golden is the
**source of truth** the shell frame-assertions in Gates 2/3 are derived from.

### Task 1.5 — Gate 1 close

Run the full .NET suite (`dotnet test`) — record the new count (was 230/0). Run the JVM suite —
must be **unchanged** (79/0): nothing Kotlin-side moved yet. Commit; push; the required
`build-test` lane must go green with the new .NET count asserted in `ci.yml`.

---

## Gate 2 — Android: Yoga owns placement

### Task 2.1 — The Yoga layout tree (Kotlin)

**Files:** create `src/BlazorNative.Jni/src/androidMain/kotlin/io/blazornative/shell/YogaLayout.kt`;
modify `WidgetMapper.kt`; instrumented test
`src/BlazorNative.Jni/src/androidTest/kotlin/io/blazornative/shell/YogaLayoutAndroidTest.kt`.

- `YogaLayout` owns `nodeId -> YogaNode`, mirrors create/insert/remove against the view tree
  (respecting the text collapse — non-negotiable #3), maps style name+value → Yoga setter, and
  runs `calculateLayout` + applies frames (`view.layout(l, t, r, b)` in **px** = dp × density).
- `SoLoader.init(context)` once at shell start (the 6.0 conclusion's Android note).
- Container for `view` becomes a plain `FrameLayout` (children are absolutely placed by Yoga).
- Style value grammar (shared with iOS): bare number | `Ndp`/`Nsp`/`Npx` | `N%` | `auto` | enum word.
  A **null value resets** the property to its Yoga default (non-negotiable: the 1.2 fix's other half).

TDD: instrumented test asserts the canonical row frames (the 6.0 spike's numbers are the template),
then a `null` reset returns a node to default.

### Task 2.2 — Native measurement (Android)

Measure func on leaves: `view.measure(MeasureSpec.makeMeasureSpec(...))` with Yoga's mode mapping
(`Exactly→EXACTLY`, `AtMost→AT_MOST`, `Undefined→UNSPECIFIED`), returning measured size ÷ density.
Instrumented test: a long `BnText` in a narrow row **wraps** and its measured height drives the row.

### Task 2.3 — Relayout on host resize

`onLayout`/`onConfigurationChanged` on the host root re-runs calculate+apply with the new bounds.
Instrumented test: change the root's size, assert frames recompute.

### Task 2.4 — Rewrite the churned instrumented tests

`WidgetMapperTest`, `BnDemoAndroidTest`, `CompositionAndroidTest`, `WidgetMapperSetStyleTest`: swap
type-pins (`LinearLayout`, `orientation == VERTICAL`) for **frame pins** (child count + each child's
computed frame). `WidgetMapperSetStyleTest`'s "unknown property → still a LinearLayout" premise is
inverted by this phase — replace it with "unknown property → logged and ignored, frames unchanged".
Keep `WidgetMapperTextChildOnButtonTest` (the collapse invariant) passing untouched.

### Task 2.5 — `BnLayoutDemo` frames on the AVD + Gate 2 close

Instrumented test asserting **every** frame of the layout demo. Bump + assert the instrumented
count in `android-instrumented.yml` (was 41). Green run required.

---

## Gate 3 — iOS: the same, via Objective-C++

### Task 3.1 — `BnYogaLayout.{h,mm}` — the plain-C node API

**Files:** create `src/BlazorNative.Apple/BnHost/BnYogaLayout.h` + `BnYogaLayout.mm`; the bridging
header `#include`s the `.h` (as `BnYogaProbe.h` already does). **Yoga headers stay inside the `.mm`.**

The surface from the design (`bn_yoga_node_new` / `insert_child` / `remove_child` / `free` /
`set_style(name, value)` — NULL value resets / `set_measure(fn, context)` / `calculate` /
`get_frame`). The **string→setter mapping and the value grammar live here, in C**, mirroring Kotlin's
exactly. Unit-test it directly from XCTest (it is plain C — callable from Swift).

### Task 3.2 — The Swift layout pass

`BnWidgetMapper`: `view` becomes a plain `UIView`; keep `nodeId -> (UIView, bn_yoga_node)`; mirror
create/insert/remove (respecting the collapse); `SetStyle` → `bn_yoga_node_set_style`; at
`CommitFrame` → `bn_yoga_calculate(root, bounds)` → apply `view.frame` from `bn_yoga_node_get_frame`.
Re-layout in `viewDidLayoutSubviews`. Measure func: a `@convention(c)` trampoline whose `void*`
context is the node identity → `view.sizeThatFits(...)`.

### Task 3.3 — Rewrite the churned XCTests

`BnRenderTests`, `BnInteractionTests`: their helpers hard-cast to `UIStackView` and index
`arrangedSubviews` — rewrite to walk `subviews` and assert **frames**. The interaction/nav
assertions themselves must keep passing unchanged in meaning.

### Task 3.4 — `BnLayoutDemo` frames on the simulator + Gate 3 close

XCTest asserting **the same numbers Gate 2 asserts** (that pairing is DoD #2 — "lays out
identically"). Bump + assert the XCTest count in `ios.yml` (was 14). Green `ios` run required.

---

## Gate 4 — Close

- Cross-platform parity stated as a **test result**: the same frame table asserted on both shells
  (the 6.0 discipline — say *how* it is asserted, not that it is true).
- `docs/plans/2026-07-13-phase-6.1-conclusion.md`: what shipped, the frame-parity proof, honest
  boundaries (scroll/picker still un-flexed → 6.2; percent/auto grammar coverage; anything the
  measure func can't do yet), the ledger for 6.2/6.3.
- Update `docs/planning/ROADMAP.md` (phase table) + `MILESTONE.md` (DoD #2/#3/#6 → ✅; record that
  **`BnStack` was consciously dropped** as a `BnColumn` synonym).
- All lanes green with counts asserted: `build-test` (.NET + JVM + 9 exports), `ios`,
  `android-instrumented`. PR → merge.
