# Phase 3.2 — Bidirectional Event Flow — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** Tap the Hello demo's button on the AVD → `@onclick` runs in the trimmed `.so` → re-render → the counter text updates on screen. M3 DoD #2 closes.

**Architecture:** `blazornative_dispatch_event` wakes up (synchronous dispatch: handler → re-render → frame callback complete before it returns; FlatJson args; 0/1/2/3 contract). Kotlin gets a single-thread dispatch lane in `BlazorNativeRuntime`; `WidgetMapper` implements its AttachEvent/DetachEvent stubs (click listener + guarded TextWatcher). `HelloComponent` becomes interactive; both golden lists update. `WasiBridge` deletes. Full contract: [2026-07-09-phase-3.2-design.md](2026-07-09-phase-3.2-design.md).

**Tech Stack:** unchanged pinned toolchain; no new dependencies.

---

## Global rules

- **Branch:** Task 0 creates `phase-3.2-events` from `main`.
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- **Environment per shell:** the usual (ANDROID_NDK_ROOT 26.3 / ANDROID_HOME / JAVA_HOME Adoptium 21 / VS-Installer on PATH).
- Baselines at branch: .NET 99/2; JVM 18; Android 23. Version bumps to `0.7.0-phase-3.2` (Exports const + both Kotlin assertions).
- Read for context: design doc; `NativeRenderer.cs:330-345` (AttachEvent emission) + `:390-413` (DispatchUiEventAsync/BuildEventArgs); `Exports.cs` dormant DispatchEvent + FlatJson access via `NativeShellBridge` internals; `HostSession.cs`; `WidgetMapper.kt` stubs; `BlazorNativeRuntime.kt`; `HelloComponent.cs`; both golden tests + their update procedures; `BridgeEventTests.cs`/`WasiBridgeTestCollection.cs`/`WasiBridge.cs` (deletion set).

## Task 0: Branch

`git checkout -b phase-3.2-events`

---

## Gate 1 — .NET (TDD) + WasiBridge deletion

### Task 1: Interactive HelloComponent + .NET golden update

1. `src/BlazorNative.Runtime/HelloComponent.cs`: add `private int _taps;` + `@onclick` equivalent in `BuildRenderTree` — on the existing button (seq 20/21 block): `b.AddAttribute(21, "onclick", EventCallback.Factory.Create<MouseEventArgs>(this, () => _taps++));` (renumber content seq as needed, keep the gap scheme; the text node becomes `$"Hello, BlazorNative! (taps: {_taps})"` — put the counter in the EXISTING fontSize-24 text so no new nodes are needed). Check `TrimSafetyProbe`'s breadcrumb — it mirrors Hello's SHAPE for trim purposes; decide (and document in a comment) whether the probe needs the event attribute too (it does NOT — trim coverage for event dispatch comes from the Gate 1 tests; note this in the probe's comment).
2. `tests/BlazorNative.Runtime.Tests/HelloGoldenTests.cs`: update the typed expected list per its documented procedure — the first frame now contains one more patch (`AttachEventPatch(buttonNodeId, "click", <handlerId>)`) and the changed text. HandlerId is runtime-assigned: assert it as `> 0` (relax that one field — Kotlin's normalize() precedent) rather than pinning; document why.
3. `dotnet test tests/BlazorNative.Runtime.Tests --filter HelloGolden` green. Commit: `feat(phase-3.2): interactive HelloComponent — @onclick tap counter; golden updated`.

### Task 2: Export contract (TDD)

1. `tests/BlazorNative.Runtime.Tests/DispatchEventTests.cs` FIRST (all RED): using the in-process pattern (HostSession-shaped graph or direct renderer),
   - `Dispatch_Click_IncrementsCounter_AndFiresReRenderFrame`: mount Hello, harvest the real handlerId from the first frame's AttachEventPatch, call the managed core (`Exports`-level testable method, split like DispatchEventNative/Core precedent) with `{"name":"click"}` → rc 0 AND a second frame arrives via `Frames` whose ReplaceText shows `taps: 1`.
   - `Dispatch_Change_BuildsChangeEventArgs`: a test component with `@onchange` capturing the value; dispatch `{"name":"change","payload":"hello"}` → component saw "hello".
   - `Dispatch_NoSession_Returns1` (fresh state), `Dispatch_MalformedArgs_Returns3` (garbage/NULL), `Dispatch_HandlerThrows_Returns2_WithVisibleDetail` (a component whose handler throws; stderr captured or detail surfaced), `Dispatch_StaleHandler_Returns0_Logged` (bogus handlerId 999999).
2. Implement in `Exports.cs`: replace the dormant body — parse args via the FlatJson parser (`NativeShellBridge` internals — hoist FlatJson to a shared internal static class `FlatJson.cs` if cleaner, updating both cross-reference comments), build `NativeUiEvent(0, (int)handlerId, name, payload)`, resolve the session renderer, `DispatchUiEventAsync(...).GetAwaiter().GetResult()` (sync contract — InlineDispatcher; document), map outcomes to 0/1/2/3. Handler exceptions: `DispatchUiEventAsync` currently swallows only `ArgumentException` (stale) — handler throws propagate through the awaited task → catch in the export → rc 2 + `ex.ToString()` to stderr. Update the Exports header (dispatch_event no longer dormant) + version const → `0.7.0-phase-3.2`.
3. All green; full `dotnet test` (record). Commit: `feat(phase-3.2): blazornative_dispatch_event live — 0/1/2/3 contract, synchronous re-render`.

### Task 3: WasiBridge deletion

1. Delete `src/BlazorNative.Core/WasiBridge.cs`, `tests/BlazorNative.Renderer.Tests/BridgeEventTests.cs`, `tests/BlazorNative.Renderer.Tests/WasiBridgeTestCollection.cs`. Check Core's generated registration (`[Singleton]` attribute was on WasiBridge — deletion removes it from `AddBlazorNativeCoreServices` automatically; verify nothing else references WasiBridge: `Select-String -Recurse "WasiBridge" src,tests` → only allowed historical comments, fix stragglers).
2. `HostSession.cs`: update the "last-wins over WasiBridge" comment → sole registration. Also check `DevHostBridge`/DevHost still build (NativeEvents stays on IMobileBridge; BN0014 still valid).
3. `dotnet build` full solution + `dotnet test` → record (expect: previous minus BridgeEventTests' count). Commit: `chore(phase-3.2): delete WasiBridge — NativeShellBridge is the sole runtime IMobileBridge`.

---

## Gate 2 — JVM through the dll

### Task 4: Kotlin dispatch lane + JVM round-trip + golden update

1. `NativeBindings.kt`: `fun blazornative_dispatch_event(handlerId: Long, argsJsonUtf8: ByteArray): Int` (+ KDoc with the 0/1/2/3 table verbatim).
2. `BlazorNativeRuntime.kt`: single-thread daemon executor (`BlazorNative-Dispatch`) + `fun dispatchEvent(handlerId: Int, eventName: String, payload: String? = null)` — builds the FlatJson args (reuse the Kotlin `FlatJson` writer), submits to the lane, rc != 0 → onError (rc 2 message says "handler threw — see native stderr… reproduce on desktop JVM"). Threading contract KDoc: ALL post-boot .NET entry serializes through this lane. Also add a `dispatchEventBlocking` internal variant returning rc for tests.
3. Republish win-x64. `ShellBridgeTest`-style new `DispatchEventTest.kt` (TDD): boot + mount Hello via the dll → harvest handlerId from the captured first frame's AttachEvent patch → `dispatchEventBlocking(handlerId, "click")` → rc 0 AND a captured second frame whose ReplaceText contains "taps: 1"; change event with payload; bogus handlerId → rc 0 (stale contract); malformed impossible from this API (writer always valid) — skip.
4. `NativeFrameAdapterTest.kt` golden: update the Kotlin expected list (+AttachEvent patch with handlerId normalized — extend `normalize()` to zero handlerIds, documented; text change).
5. `./gradlew testDebugUnitTest --rerun` all green (record). Commit: `feat(phase-3.2): Gate 2 GREEN — click round-trips through the dll; goldens updated`.

---

## Gate 3 — Android

### Task 5: WidgetMapper listeners + MainActivity + instrumented round-trip

1. `WidgetMapper.kt`: constructor gains `private val onUiEvent: (handlerId: Int, eventName: String, payload: String?) -> Unit = { _, _, _ -> }` (default no-op keeps existing tests compiling). Implement the stubs:
   - AttachEvent: `"click"` → `view.setOnClickListener { onUiEvent(p.handlerId, "click", null) }`; `"change"` on EditText → TextWatcher firing `onUiEvent(p.handlerId, "change", s.toString())`, stored via `view.setTag(R.id.<new tag id> or a tag map)` keyed by handlerId; re-entrancy guard: an `applyingBatch` flag set around `applyBatch` — the watcher returns early while true (programmatic setText during patch application must not dispatch).
   - DetachEvent: clear click listener / remove watcher by handlerId. Unknown names: `Log.w` + skip. Update the class KDoc (stubs are live; Phase 3.2).
2. `MainActivity.kt`: `WidgetMapper(this, widgetRoot, onUiEvent = { h, n, p -> runtime.dispatchEvent(h, n, p) })`.
3. Republish both bionic RIDs; verify dispatch_event still exported (it was — body changed only).
4. Instrumented `EventRoundTripAndroidTest.kt`: launch MainActivity via ActivityScenario (WidgetMapperTest pattern), poll for the button, `runOnUiThread { button.performClick() }`, poll for text containing "taps: 1" (10s); tap again → "taps: 2" (proves re-attach after re-render); change test: `runOnUiThread { editText.setText("abc") }` → poll logcat-free: the change dispatch fires (assert via a second render or simply that no crash + rc path — simplest observable: the .NET side has no change handler in Hello, stale-handler rc 0 is fine — INSTEAD assert the watcher fired by tracking through a test tap... keep it simple: the change-event end-to-end was proven on JVM in Gate 2; the instrumented change test just proves the watcher fires without crashing and the re-entrancy guard holds during patch application (tap → re-render → setText on the text node → watcher must NOT dispatch). Assert via WidgetMapper state or a counting onUiEvent wrapper injected in a dedicated non-Activity test with synthetic frames — the Phase 2.6 synthetic-fixture pattern.)
5. Filtered + full `connectedAndroidTest` (record; expect 23 baseline + new). Commit: `test(phase-3.2): Gate 3 GREEN — tap round-trip live on AVD`.

---

## Gate 4 — Docs

### Task 6: Close-out

Full sweep all three surfaces (record). `docs/plans/2026-07-09-phase-3.2-conclusion.md` (house style: gates, counts, contract-as-built, carryovers: full DoD #9 strict mode; @bind/DoD #5 next natural phase; focus/blur unwired; dispatch-lane starvation watch). ROADMAP: 3.2 ✅, **M3 DoD #2 CLOSED**; next-pointer to the component-library phases (3.3+: Bn* library DoD #4, @bind DoD #5, nested components DoD #8, navigation DoD #7). MILESTONE: DoD #2 ✅ closed note + DoD #9 partial note. Commit: `docs(phase-3.2): Phase 3.2 GREEN close-out — M3 DoD #2 closed`. Controller merges after final review.
