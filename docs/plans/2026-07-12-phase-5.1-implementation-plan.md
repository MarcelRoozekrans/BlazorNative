# Phase 5.1 — Host-Initiated Events — Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task.

**Goal:** host-originated events reach .NET (M5 DoD #5): a 9th C-ABI export carries
lifecycle/back/deep-link events; Android lifecycle wires into it; predictive back →
navigation-back; a deep link starts the app on the linked route — proven on the AVD.

**Architecture:** per [2026-07-12-phase-5.1-design.md](2026-07-12-phase-5.1-design.md).
Version `1.3.0-phase-5.1`. ABI grows 8→9. Merge via PR.

---

## Global rules

- **Branch:** `phase-5.1-host-events` from `main` (controller creates).
- **RED-on-ambiguity:** one diagnostic iteration per failure, then stop and report.
- **Environment:** the usual (NDK 26.3 / ANDROID_HOME / JAVA_HOME Adoptium 21 /
  VS-Installer PATH); AVD `blazornative-pixel6-x86_64` for Gate 3.
- Baselines: .NET 203/0, JVM 73, Android 35 (all grow this phase; assertions updated).
  **Eight exports → nine** — the assertion sites: `ci.yml` (3 RID steps),
  `android-instrumented.yml`, `ios-spike.yml`, plus `NativeBindings.kt` (binding) and
  any current-state doc saying "eight-export".
- Read for context: design doc; `src/BlazorNative.Runtime/Exports.cs` (the 8 exports +
  `DispatchEventCore` split pattern + rc contracts); `NativeShellBridge.cs:304-313`
  (the no-op stub to replace) + `DevHostBridge.cs:110-137` (RaiseNativeEvent/InjectEvent
  — the firing pattern to mirror); `IMobileBridge.cs:30,54` (NativeEvents + NativeEvent
  type); `NativeNavigationManager.cs` (NavigateToAsync, QueryStartupRoute, RaiseRouteChanged)
  + `HostSession.cs` SwapRoot/RunAfterDispatch; `FocusProbe.cs` + registry (HostEventProbe
  precedent); `MainActivity.kt` (onCreate/EXTRA_COMPONENT) + `BlazorNativeRuntime.kt`
  (dispatch lane, dispatchEvent, the shutdown-in-onDestroy warning) + `AndroidShellBridge.kt`
  (the route slot for deep links) + `AndroidManifest.xml`; `NavigationAndroidTest.kt` +
  `FocusBlurAndroidTest.kt` (instrumented patterns; moveToState is NEW).

## Task 0: Branch + the owed ROADMAP flip

`git checkout -b phase-5.1-host-events`. In the FIRST commit (with the design/plan docs
already on main? no — they land here): flip ROADMAP Phase 5.0 ⏳→✅ (the spike merged
without its status marker; add the DoD #1 closure line + conclusion link) and confirm
MILESTONE DoD #1 shows closed. Commit: `docs(phase-5.1): design + plan; ROADMAP Phase 5.0 ✅ (owed marker)`.

---

## Gate 1 — .NET + ABI (TDD)

### Task 1: 9th export + real NativeEvents + host-event core

1. **FIRST** verify `RunAfterDispatch` behaves off the dispatch lane (no open batch):
   a test that calls a swap-style deferred action with no dispatch in flight → it
   drains correctly. If it doesn't → STOP and report (the back path depends on it).
2. TDD: `DispatchHostEventCore(name, payload)` rc contract (0/2/3) in the Exports test
   area; `NativeShellBridge` real `NativeEvents` firing + per-subscriber isolation
   (mirror DevHostBridge tests); assert BN0014 analyzer tests still green.
3. Implement `blazornative_host_event` export (cdecl + EntryPoint, top-level try/catch —
   BN0020/21 clean) → `DispatchHostEventCore`. Replace the NativeShellBridge stub with
   the real multicast + RaiseNativeEvent. Header → "Nine exports".
4. Bump every export-count assertion 8→9: `ci.yml` (3 steps), `android-instrumented.yml`,
   `ios-spike.yml`; add the `blazornative_host_event` JNA binding to `NativeBindings.kt`;
   update current-state "eight-export" doc mentions (README) → nine.
5. Version const → `1.3.0-phase-5.1` + both Kotlin assertion files.
6. Win-x64 publish → dumpbin shows exactly **9** `blazornative_*` exports + 4 IL2072s.
   Full `dotnet test` → record; update ci.yml .NET count assertion same commit.
7. Commit: `feat(phase-5.1): blazornative_host_event (9th export) + real NativeEvents firing (DoD #5 ingress)`.

### Task 2: NavigateBack + previous-route + HostEventProbe

1. TDD `NavigationTests.cs`: `_previousRoute` set on NavigateToAsync; `NavigateBackAsync`
   returns true + swaps to prior; at root returns false; back-then-back exhausts the slot.
2. `INavigationManager.NavigateBackAsync` (Core) + impl (Runtime).
3. `HostEventProbe.cs` (internal, registry "HostEventProbe"): `[Inject] IMobileBridge`,
   subscribes NativeEvents in OnInitialized (sync), renders last-event `BnText`,
   unsubscribes in Dispose. TDD: mount + `DispatchHostEventCore("onPause")` → probe
   BnText shows "onPause" (nodeId-pinned); BnDemo golden unchanged.
4. Full `dotnet test` → record; ci.yml count. Commit:
   `feat(phase-5.1): NavigateBack (previous-route slot) + HostEventProbe consumer (DoD #5 .NET)`.

---

## Gate 2 — JVM

### Task 3: host_event + NavigateBack through the dll

Fresh win-x64 publish. `BlazorNativeRuntime.dispatchHostEvent(name, payload?)` (parallels
dispatchEvent, on the lane) + a blocking test seam. New `HostEventTest.kt`: mount
HostEventProbe → `dispatchHostEvent("onResume")` → probe BnText ReplaceText pinned;
NavigateBack through the dll (navigate to settings via button, then a back dispatch →
BnDemo returns). Verify the 9-symbol export surface (extend the boot smoke or a symbol
assertion). `gradlew testDebugUnitTest --rerun` → record; ci.yml JVM count.
Commit: `feat(phase-5.1): Gate 2 GREEN — host_event + NavigateBack through the dll`.

---

## Gate 3 — Android

### Task 4: Lifecycle + predictive back + deep link + instrumented

1. `MainActivity`: `onPause`/`onResume`/`onDestroy` → `runtime.dispatchHostEvent(...)`
   (onDestroy fires the event but does NOT shutdown — document the trap);
   `OnBackInvokedCallback` (API 33+) + `onBackPressedDispatcher` fallback → dispatchHostEvent
   routing to NavigateBack, `false`→default finish; `onCreate` parses `intent.data`
   (`blazornative://<route>`) → feeds the AndroidShellBridge route slot before boot.
2. `AndroidManifest.xml`: `VIEW` intent-filter (`blazornative` scheme);
   `android:enableOnBackInvokedCallback="true"`.
3. Republish both bionic RIDs → llvm-readelf shows **9** exports (4 IL2072s each).
4. New `HostEventAndroidTest.kt` (EXTRA_COMPONENT=HostEventProbe): `moveToState(STARTED)`
   → probe shows "onPause" on-screen. New deep-link test: `ActivityScenario.launch(VIEW
   intent, blazornative://settings)` → BnSettingsPage at startup. Predictive-back test:
   navigate to settings → trigger back → BnDemo returns fresh. Filtered + full
   `connectedAndroidTest` → record. `dotnet test` + JVM no-regression.
5. Commit: `test(phase-5.1): Gate 3 GREEN — lifecycle + predictive back + deep link on the AVD (DoD #5 closed)`.

---

## Gate 4 — Close-out + PR

### Task 5: Docs + closure

1. `docs/plans/2026-07-12-phase-5.1-conclusion.md` (house style; the 9th-export ABI
   decision + ripple, the previous-route back semantics, deep-link-launch-only +
   custom-scheme honesty, HostEventProbe scaffolding, the onDestroy-trap handling,
   carryovers: onNewIntent while-running nav = M5.3/later, https App Links = M7, back
   stack beyond one slot = as-needed).
2. ROADMAP Phase 5.1 ✅; MILESTONE DoD #5 ✅ closure note (lifecycle + back + deep links,
   all on the AVD; NativeEvents fork closed). Issues #16/#19 comment trail.
3. Commit: `docs(phase-5.1): Phase 5.1 GREEN close-out — DoD #5 closed`.
4. Controller: final whole-branch review → PR (closes #19; #16 narrowed) → CI green → merge.
